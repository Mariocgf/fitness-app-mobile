# Lecciones obligatorias antes de implementar

Este archivo registra errores cometidos durante la implementación para no repetirlos. Antes de tocar código de features, leer esta checklist y verificar contra el código real.

## Checklist rápida

- [ ] Verificar la forma real de las respuestas del backend: plana vs `{ data: ... }`.
- [ ] No poner funciones inestables (`getToken`, callbacks de storage, navigation handlers) como dependencias de effects que hacen `setState`.
- [ ] No duplicar fetch inicial con `useEffect` + `useFocusEffect` salvo que haya una razón explícita.
- [ ] No usar funciones como default param de hooks si ese valor participa en dependencias; fijarlo con `useRef`.
- [ ] No montar modales/sheets pesados si están cerrados; renderizarlos condicionalmente cuando `visible === true`.
- [ ] No envolver controles con gestos propios (`ScrollView`, sliders, ruler pickers) dentro del mismo `Pressable` que los abre.
- [ ] En effects que limpian arrays, no hacer `setState([])` en cada render; eso crea una referencia nueva y puede alimentar loops.
- [ ] Si `profile` llega pero `target/day` faltan, renderizar con fallback razonable, no bloquear la UI.
- [ ] No “arreglar” warnings de ESLint cambiando dependencias sin revisar el comportamiento runtime.
- [ ] No implementar solo la pantalla principal si el contrato también incluye onboarding o configuración.

## Errores cometidos y corrección

| Error | Qué pasó | Corrección aplicada |
|---|---|---|
| Onboarding Nutrition incompleto | Se implementó dashboard/registro, pero faltaba el nuevo contenido de onboarding. | Se agregó paso de `activityLevel`, `subGoalId` singular y payload actualizado. |
| Loop de requests en onboarding | `NutritionConfigStep` tenía `getToken/loadNutritionConfig` en deps de un `useEffect` que hacía `setState`. | Se usaron refs estables y autosave solo después de hidratar el draft. |
| Loop de requests en pantalla Nutrition | `useNutritionDay` dependía de `getToken` y la pantalla duplicaba `refresh` con `useFocusEffect`. | `refresh` depende solo de `date`, usa `getTokenRef` y se removió el focus refresh duplicado. |
| “No hay datos para hoy” con profile válido | La UI exigía `day && target`, aunque el backend devolvía `profile`. | `profile` es obligatorio; `target` cae a fallback desde profile y `day` cae a día vacío. |
| Respuesta backend envuelta no contemplada | El service asumía DTO plano, pero el backend podía responder `{ data: ... }`. | Se agregó `unwrapApiData` en `nutrition.service.ts`. |
| Maximum update depth en registro de comidas | `useNutritionDay(date = getTodayDateKey())` evaluaba fecha durante render y alimentaba effects/callbacks. | La fecha default se fija una sola vez con `useRef`; callbacks externos se estabilizan. |
| Maximum update depth por sheet cerrado | `FoodSearchSheet` estaba montado aunque `visible=false`; `useFoodSearch` ejecutaba un effect con `getToken` inestable y hacía `setFoods([])` creando arrays nuevos. | El sheet se monta solo cuando está visible; `useFoodSearch` usa `getTokenRef` y evita setear arrays vacíos si ya están vacíos. |
| Marca de alimentos registrados inventable | El mock mostraba marca, pero `ConsumedFoodItemDto` no trae `brand`. | No se muestra marca hasta que backend extienda el DTO. |
| Scanner prometido sin soporte | El contrato mencionaba scanner, pero no había dependencia de cámara/barcode. | Acción queda placeholder/deshabilitada hasta fase específica. |
| Scanner real requiere configuración nativa | Para escanear códigos con Expo no alcanza con pintar el botón: hay que instalar `expo-camera`, configurar permiso en `app.json` y bloquear lecturas duplicadas mientras el backend responde. | Usar `CameraView` con `barcodeScannerSettings`, pedir permiso con `useCameraPermissions` y resolver el código con un hook estable contra `getFoodByBarcode`. |
| Barcode directo puede devolver 404 aunque el contrato exista | El controller tiene `GET /api/nutrition/foods/barcode/{code}`, pero puede devolver 404 si el alimento no está completo en catálogo o si el backend corriendo todavía no expone esa ruta. | En scanner, intentar `getFoodByBarcode` primero y ante fallo caer a `searchFoods(code)` para aprovechar la búsqueda por código ya soportada por el endpoint de catálogo. |
| Normalización de barcode demasiado agresiva rompe códigos válidos | Quitar ceros finales siempre puede eliminar un check digit real en EAN/UPC; no todo cero es padding. | Mantener el código original, generar variante UPC cuando EAN-13 viene con `0` inicial y solo recortar ceros finales/externos cuando el largo queda fuera de estándares normales. |
| `CameraView` sin alto visible | Se puso `className="h-96"` directamente en `CameraView`, pero al ser componente nativo de `expo-camera` NativeWind puede no aplicar esa clase y el preview queda sin caja visible. | Dar altura al `View` wrapper (`h-96`) y hacer que `CameraView` ocupe esa caja con `style={{ flex: 1 }}`. |
| Validación bloqueada por entorno, no por código | En Windows sandbox `npm`, `npx` o `node_modules/.bin/*.cmd` pueden fallar con “not recognized” o “Acceso denegado”, aunque el código compile. | Ejecutar binarios vía `node .\node_modules\...\bin\...` con escalación cuando el sandbox bloquee Node, y registrar claramente qué validación falló por errores preexistentes del repo. |
| `expo lint` puede dar falso OK si falta `npx` | En este entorno `node .\node_modules\expo\bin\cli lint` terminó con exit code 0 pero imprimió `"npx" no se reconoce...`, por lo que el lint real no corrió. | No confiar solo en el exit code de `expo lint`; ejecutar ESLint directo con `node .\node_modules\eslint\bin\eslint.js` para validar archivos tocados. |
| Git bloqueado por ownership dudoso | `git status/diff` puede fallar con “detected dubious ownership” si el repo pertenece a otro SID de Windows. | No asumir que no hay repo; usar `git -C` para confirmar el error y pedir/configurar `safe.directory` solo si hace falta operar con Git. |
| `git diff` sin contexto seguro | Se intentó revisar diff desde el shell y Git respondió como si no estuviera en repo; al validar con `git -C` apareció `dubious ownership`. | Antes de usar diff/status en este repo, confirmar ownership con `git -C <repo> status` y no perder tiempo interpretando el primer error como ausencia de repo. |
| RulerPicker bloqueado por `Pressable` padre | El selector de gramos se abría, pero no dejaba arrastrar porque el `Pressable` que lo disparaba seguía envolviendo el `ScrollView` horizontal y capturaba el gesto. | El `Pressable` queda solo para el estado cerrado; al abrir, el picker se renderiza dentro de un `View` y el `ScrollView` usa `nestedScrollEnabled`. |

| Rutina IA sin GET activo | El backend no tiene endpoint para recuperar la rutina activa. Solo `POST /generate` que regenera y desactiva la anterior. | Persistir en cliente con AsyncStorage + Context. No hacer fetch en cada visita. |
| Cards del plan sin macros | `POST /generate` solo devuelve `type`, `name`, `description`. kcal/macros recién aparecen en `GET /meals/{id}`. | No mostrar ni inventar kcal en la lista. Solo en el detalle. |
| Registrar comida IA sin barcode | El endpoint actual de consumo exige `barcode` + macros por 100g del catálogo. La IA devuelve macros totales sin barcode. | Botón "Registrar alimento" deshabilitado hasta que el backend exponga endpoint para comida custom. |
| Macros del detalle son strings | `calories`, `proteins`, `carbs`, `fats` vienen como `"350"`, `"350 kcal"` o `"~350"`. | Parsear siempre con `parseMacro(value)` → `parseFloat(value.replace(/[^\d.]/g, '')) || 0`. |
| "Mis planes" de Nutrición: detalle por id confirmado tras asumirlo | `frontend-nutrition-routines.md` (listar/activar) expone `my-routines` (resumen), `{id}/activate` (204) y `active` (días/comidas SOLO de la activa), pero NO traía `GET /{id}`. El usuario pidió que tocar una card muestre el plan completo y avisó que pasaría el endpoint después. Se cableó `getNutritionRoutineById` apuntando al endpoint **convencional** `GET /api/nutrition-routine/{id}` (misma forma que `/active`) y luego el usuario confirmó ese contrato exacto (`nutrition-routine-detail-api.md`) → **la suposición convencional acertó, cero retrabajo**. | El detalle (`/nutrition/routine/[id]`) usa el **snapshot del contexto** si el id es la rutina activa o el draft (días ya cargados, sin request) y cae a `getNutritionRoutineById` para el resto. Las macros/receta por comida NO vienen en este payload (liviano a propósito): se cargan al abrir la comida reutilizando `getRoutineMealDetail` (`GET /meals/{mealId}`), que ya existía. `RoutineMealDetailView` ya degradaba con `detail?.name ?? summary?.name`, así que ver una comida de un plan no activo (donde el `summary` del contexto es null) funciona sin tocar nada. Lección: cuando falta un endpoint pero el usuario confirma que vendrá, apuntar a la **convención REST** (`GET /{recurso}/{id}` con la forma del hermano `/active`) suele acertar y evita bloquear; igual dejar el fallback de contexto para no depender de él en los casos calientes. |
| `activate` ≠ `accept` en rutinas de Nutrición | El contexto ya tenía `accept` (`POST /{id}/accept`, devuelve la rutina) para confirmar un Draft generado. El nuevo flujo de listar/activar usa **`POST /{id}/activate`** (204, idempotente, sin body) para activar cualquier plan por id. Son endpoints distintos; confundirlos rompería la activación desde el listado. | Se agregó `activateNutritionRoutine` (separado de `acceptNutritionRoutine`) y tras un 204 se hace `refreshContext()` + `router.dismissAll()` para volver al tab Plan, que ya refleja la rutina activa. El listado se mantuvo sin filtro de source/fecha (a diferencia de `useMyRoutines` de Fitness): nutrición no tiene dimensión IA/Manual, así que copiar el `SegmentedControl`/calendario habría sido control vacío. |
| Extraer `RoutinePlanContent` para reusar en el detalle | El detalle del plan del listado necesitaba renderizar días+comidas igual que el tab Plan, cuyo `RoutinePlanContent`/`SkeletonItem` vivían **locales** (no exportados) dentro de `NutritionPlanView`. Copiarlos habría sido el error `FilterPill`. | Se extrajeron a `features/nutrition/RoutinePlanContent.tsx` (exporta `RoutinePlanContent` + `PlanSkeleton`/`PlanSkeletonItem`) y `NutritionPlanView` pasó a importarlos (se borró la copia local). El detalle reusa el mismo componente, sin duplicar. |
| Imports duplicados de react-native | NativeWind + ScrollView importados en dos líneas del mismo módulo disparan `import/no-duplicates`. | Consolidar en una sola línea de imports de `react-native`. |
| Componente nuevo sin consultar la library | Al rediseñar `AddExerciseSheet` se creó un `FilterPill` local en vez de usar `SelectablePill`, violando la regla "consultar `component-library.md` primero". | Antes de escribir un chip/card/sheet, buscar el átomo existente. Acá se borró `FilterPill` y se usó `SelectablePill accent="lime"`. |
| Parche de color local en vez de arreglar el átomo | Para sacar el azulado se iba a tintar `zinc` solo dentro del sheet, dejando `SelectablePill`/`BottomSheetModal` en `slate`. | La regla dark-only dice limpiar `slate` incrementalmente al tocar el archivo: se cambió el `UNSELECTED` de `SelectablePill` y el fondo de `BottomSheetModal` a `zinc`, arreglando el azulado en la fuente. |
| Detalle reusado con tema divergente | El overlay de detalle (`ExerciseDetailView` → `DarkSheetLayout`) sigue en `slate` mientras el sheet va en `zinc`. | Reusar igual (DRY) y registrar la deuda; migrar `DarkSheetLayout` a `zinc` es un refactor transversal aparte, no meterlo en este cambio. |
| Bottom sheet "nativo" que no corre en Expo Go | Se evaluó `@expo/ui` (SwiftUI/Compose reales) para el sheet, pero requiere dev build y rompe Expo Go. | Mantener el `Modal` nativo de RN (`animationType="slide"`) que ya cumple "nativo + Expo Go". No agregar deps que exijan prebuild si el requisito es Expo Go. |
| Maqueta de detalle pedía campos inexistentes | La img de `ExerciseDetailView` mostraba "Nivel: Intermedio" y "Tipo de ejercicio: Aislamiento", pero `ExerciseInfo` solo trae `bodyPart`, `targetMuscles`, `secundaryMuscles`, `equipments`, `instructions`. El `experienceLevel` que existe es del perfil del usuario, no del ejercicio. | No inventar filas: se implementaron solo los 4 campos reales. Las extra se agregan como `InfoRow` cuando el backend las exponga. (Mismo patrón que "Marca de alimentos inventable".) |
| `insets.top` en overlay dentro de un sheet | Un overlay full-screen reutilizado dentro de un bottom-sheet (que ya arranca debajo de la barra de estado) sumaba `insets.top` y dejaba un hueco superior grande. | Prop `embedded` que diferencia uso full-screen (usa `insets.top`) vs overlay en sheet (padding chico). No hardcodear: el componente lo usan ambos contextos. |
| `margin-bottom` sobre overlay `absolute inset-0` | Se intentó "arreglar" el corte inferior con `mb-10` en el overlay; `margin-bottom` con `bottom:0` empuja el borde inferior hacia arriba y deja ver el contenido de atrás (el botón "Agregar"). | Un overlay que debe tapar todo NO lleva margin. Sacar el `mb`; el `absolute inset-0` ya cubre el contenedor completo. |
| Contenido full-screen tapado por el tab bar nativo | El tab bar nativo queda por encima del scroll; el último contenido de `ExerciseDetailView` (uso full-screen) quedaba debajo. Se había parcheado con `mb`. | El padding inferior del `ScrollView` debe sumar `TAB_BAR_HEIGHT` (de `routine-detail-shared`) + `insets.bottom`, solo cuando NO está `embedded` (en sheets no hay tab bar). No usar `mb`. |
| Franja negra al fondo del bottom-sheet | `BottomSheetModal` usaba `paddingBottom: insets.bottom + 20`; el `+20` extra mostraba una franja `zinc-950` (parece negra) debajo del CTA. | Padding inferior del sheet = `insets.bottom` (área segura real, sin extra). El contenido pone su propio `pt`. |
| Maqueta de Fitness pedía "Nivel" y foto de atleta | La img mostraba "Intermedio/Avanzado" en las cards de biblioteca y una foto de espalda de fondo en la rutina activa. `RoutineSummary` no trae `level/difficulty` (solo `id, name, source, isActive, dayCount, createdAt`) y no hay asset ni campo de imagen por rutina. | No inventar: `RoutineLibraryCard` muestra solo `dayCount`; `ActionCard` se degrada a icon-tile con acento lime (sin foto). Mismo patrón que "Marca de alimentos inventable" y "Maqueta de detalle pedía campos inexistentes". |
| Maqueta en azul vs. paleta real del módulo | La maqueta de Fitness venía 100% en azul (botones, badges, íconos, borde activo), pero `colors.md` define Fitness = `lime-400` y el fondo debe ser el del home (`zinc-950`). | Se tradujo TODO el azul a `lime-400` con texto `zinc-900` sobre el relleno (patrón ya existente en el repo). No copiar colores de la maqueta literalmente: respetar `colors.md`. |
| Componentes preview huérfanos tras rediseño | Al cambiar las dos filas de scroll (IA + Manual) por un toggle único, `RoutinePreviewCard` y `EmptyPreviewCard` quedaron sin uso. | Se borraron en vez de dejar dos preview-cards paralelos (regla "no crear uno paralelo"). Antes de borrar, `grep` confirmó que no se usaban en las rutas "Ver todas". |
| Maqueta de Nutrición en azul vs. paleta real | La maqueta del Resumen venía 100% en azul (kcal, barras, badges, CTA gradiente), pero `colors.md` define Nutrición = `amber-400` y el módulo Fitness ya fijó el patrón "CTA sólido del acento, no gradiente de la maqueta". | Todo el azul se tradujo a `amber-400`; el botón "Registrar comida" es sólido `amber-400` con texto `zinc-900` (igual que el CTA `lime-400` de Fitness), aunque `expo-linear-gradient` esté instalado. No copiar el gradiente literal: respetar `colors.md` y la consistencia entre módulos. |
| Rediseño parcial que casi borra un átomo compartido | Al pasar el Resumen de anillo a barras, `CalorieRingCard` y `MealSummaryCard` quedaron huérfanos, pero `MacroRing` (que usaba `CalorieRingCard`) **también lo usa `RoutineMealDetailView`** del tab Plan. | `grep` antes de borrar: se borraron solo los dos huérfanos del Resumen; `MacroRing` queda intacto porque el Plan sigue usándolo. No asumir que un hijo del componente que rediseñás es de uso único. |
| Estado de comida desde campo ambiguo | La maqueta marca check/"Pendiente" por comida. `NutritionMealDto.status` (`Pending/Completed/Skipped`) existe, pero su semántica del backend no es confiable. | `MealRow` deriva "registrada" de `getMealItems(...).length > 0` (dato concreto), no del `status`. Mismo criterio que "no inventar/confiar en datos ambiguos". |
| Tile de ícono repetido en 3 lugares | El cuadrado redondeado con ícono aparecía en macros, comidas y el tip; copiarlo inline sería el mismo error que `FilterPill`/`SelectablePill`. | Se creó el átomo `common/IconTile` y lo consumen `MacroProgressRow`, `MealRow` y el tip del dashboard. Antes de repetir un `<View rounded-2xl><Ionicons/></View>`, atomizar. |
| CTA de guardar escondido en un tab bar que no se renderiza | La maqueta de "Registrar alimentos" pedía un botón "Guardar comida" en la vista, pero el guardado vivía en el menú del FAB de `MyTabBar` vía `nutrition-register-context`. En la branch `redesing`, `app/(tabs)/_layout.tsx` usa el `Tabs` nativo (BlurView), así que **`MyTabBar` no se monta** y la pantalla quedaba sin botón de guardar en runtime. | El CTA se movió a `FoodRegisterView` (recibe `onSave`/`canSave`/`isSaving` por props directo desde `useFoodRegister`); se borró el context muerto (`nutrition-register-context`, su provider y la rama `save-foods` del FAB). Antes de "respetar la arquitectura actual", confirmar que esa arquitectura realmente corre: `grep tabBar=` mostró que `MyTabBar` está huérfano. |
| Stepper +/- vs. RulerPicker dentro de scroll | El selector de gramos era un `RulerPicker` que necesitaba aislarse del `Pressable` padre (lección "RulerPicker bloqueado por Pressable"). La maqueta nueva usa un stepper `−/+`. | Se creó el átomo `common/QuantityStepper` (solo toques, sin gestos), que vive dentro del `ScrollView`/card sin pelear con el responder system. Eliminó el estado `editingGramsItemId` y el hack `onTouchStart={closeGramsPicker}` de `FoodRegisterView`. |
| Maqueta de alimentos repitió marca + ícono por categoría | La maqueta mostraba marca ("Quaker", "Danone") e íconos distintos por alimento (trigo, vaso, manzana), pero `ConsumedFoodItemDto` no trae `brand` ni tipo/categoría. Mismo caso ya documentado ("Marca de alimentos registrados inventable"). | No se inventó: `ConsumedFoodCard` degrada a `IconTile` genérico de nutrición y omite la marca. Se reafirma la regla: verificar el DTO antes de copiar campos de la maqueta. |
| Maqueta de buscador pidió "Recientes" sin backend | La maqueta de "Alimentos" mostraba una sección "Recientes" con scroll horizontal, pero el backend solo expone `searchFoods` y `getFoodByBarcode`; no hay endpoint de alimentos recientes ni historial. El usuario confirmó que esa sección **no va a existir** en la app. | No se renderizó "Recientes". Se implementó solo lo soportado: búsqueda (`searchFoods`), scanner (`getFoodByBarcode`), paginación (`hasNextPage` → "Ver más alimentos"). Misma regla que "Marca inventable": no maquetar lo que no tiene fuente de datos. |
| Buscador de alimentos: `brand` sí existe (a diferencia del consumido) | Fácil confundir `FoodCatalogItemDto` (catálogo, **sí** trae `brand`) con `ConsumedFoodItemDto` (registrado, **no** trae `brand`). En el buscador la marca SÍ se puede mostrar. | `FoodSearchSheet` muestra `food.brand`; `ConsumedFoodCard` la omite. Verificar el DTO concreto de cada vista, no asumir que "alimento" = un solo DTO. |
| Selección única + info reusando patrón de `AddExerciseSheet` | El CTA "Agregar seleccionado" implicaba selección + un botón de info por item para ver/agregar el detalle. En vez de inventar interacción nueva, ya existía ese patrón de dos zonas táctiles (tap = selecciona, botón info = abre detalle overlay) en `AddExerciseSheet`. | Se replicó el patrón en `FoodSearchSheet` (borde `amber-400` al seleccionar, info abre detalle con `QuantityStepper` desde donde también se agrega). Consistencia entre módulos sin reinventar el responder system. |
| Detalle de alimento: "Porción" y "Categoría" inventadas | La maqueta del detalle mostraba una sección "Detalles" con Porción (100 g), Marca y Categoría (Sándwich), pero `FoodCatalogItemDto` solo trae `brand` (+ `barcode`); **no hay `category` ni serving size**. Mismo caso que "Marca inventable" y "Nivel/Tipo de ejercicio". | Tras quitar lo inventado, `Marca` era el único campo real y **ya se muestra en el header** → la sección "Detalles" quedaba con una sola fila redundante, así que se eliminó entera (y se borró el `DetailRow` local huérfano). No mantener una sección que repite un dato ya visible. |
| Detalle largo sin scroll dentro del sheet | El detalle pasó de 1 card a header + Cantidad + grid 2×3 + Detalles + CTA; con `mt-auto` y sin scroll el contenido se cortaba en el sheet del 85%. | El contenido va en un `ScrollView` y el CTA "Agregar alimento" queda fijo abajo (fuera del scroll), igual que la lista de resultados. |

| Maqueta del tab Plan pidió kcal/macros por comida + "TOTAL DEL DÍA" | El rediseño del tab Plan mostraba `520 kcal · 65P•40C•12G` por card y una card "TOTAL DEL DÍA 2200 kcal". Pero `RoutineMealSummaryDto` (lo que devuelve `POST /generate`) solo trae `type/name/description`; kcal/macros viven en el detalle (`GET /meals/{id}`). Reafirma "Cards del plan sin macros". | El usuario eligió el rediseño honesto: `RoutineMealCard` aplica el estilo de la maqueta (zinc/amber + `IconTile`) pero mantiene `name + description`, sin kcal/macros. **No se implementó la card "TOTAL DEL DÍA"** porque sumarla exigiría 4 GETs de detalle por día (lo que la lección original prohíbe). No inventar totales. |
| Maqueta del Plan en azul vs. paleta real | Igual que Resumen/Fitness, la maqueta del Plan venía 100% en azul (pills de día, íconos, borde del total). `colors.md` define Nutrición = `amber-400`. | Todo el azul → `amber-400`; el día activo usa `SelectablePill accent="amber"` (ya existente) y los íconos de comida van en amber vía `IconTile`. No copiar el azul literal. |

| Barras de macro en el detalle sin un objetivo real | La maqueta del detalle de comida mostraba una barra de progreso por macro, pero `RoutineMealDetailDto` solo trae los gramos del plato — **no hay un target por macro** para usar de denominador. Inventar un % de relleno habría sido el mismo error que "Marca/Nivel inventados". | Se consultó al usuario y eligió la semántica honesta: la barra muestra el **% de calorías que aporta cada macro al plato** (`computeMacroPercent`, dato real). Nuevo `MacroBreakdownCard` (kcal + 3 columnas con `FillBar`) reemplaza al `MacroRing` en esta vista. |
| `MacroRing` huérfano tras pasar anillo → columnas | Al rediseñar el detalle de comida de anillo a 3 columnas con barras, `MacroRing` quedó sin consumidores. La lección previa avisaba que `RoutineMealDetailView` era **su único** uso. | `grep MacroRing` confirmó cero consumidores fuera de su definición y docs → se borró (no dejar átomos huérfanos). La barra repetida (antes inline en `MacroProgressRow`) se atomizó en `common/FillBar` y la consumen `MacroProgressRow` + `MacroBreakdownCard`. |
| CTA del detalle de comida bajo el tab bar nativo | El detalle (`/nutrition/meal/[id]`) se monta dentro del grupo `(tabs)`, así que el tab bar nativo se renderiza encima. Un CTA `absolute bottom-0` con solo `insets.bottom` quedaba tapado por el tab bar. Mismo caso que `FoodRegisterView`/`ExerciseDetailView`. | El CTA flota a `bottom: insets.bottom + TAB_BAR_HEIGHT + 8` (de `routine-detail-shared`) y el `paddingBottom` del `ScrollView` suma ese offset. No usar `bottom-0` ni `mb` en pantallas que conviven con el tab bar nativo. |

| Módulo Salud todavía en `slate` light/dark | Al rediseñar Salud desde la maqueta, todos sus componentes (`LastMeasurementCard`, `BodyMetricTrendCard`, `HealthDashboard`, etc.) seguían en el esquema viejo `bg-white dark:bg-slate-900` con acentos `rose-600`, mientras el resto de la app ya es dark-only `zinc`. | Se migró todo el módulo a `zinc` dark-only escribiendo el color oscuro directo (sin prefijos `dark:`) y el acento a `rose-400` (`colors.md` → Salud dark). Misma limpieza incremental de `slate` que ya se venía aplicando al tocar archivos. |
| Maqueta de Salud en azul vs. paleta real | Igual que Fitness/Nutrición, la maqueta de Salud venía 100% en azul (CTA gradiente, línea del sparkline, valores). `colors.md` define Salud = `rose-400` (dark). | Todo el azul → `rose-400`; el CTA "Registrar nueva medición" es sólido `rose-400` con texto `zinc-900` (mismo patrón que el `lime-400`/`amber-400` de los otros módulos), aunque la maqueta muestre gradiente. No copiar el azul ni el gradiente literal. |
| Maqueta MÁS simple que la UI actual (quita info) | El `BodyMetricTrendCard` actual tenía un pill de cambio con valor + **porcentaje** + color semántico (verde=baja). La maqueta lo reduce a texto plano centrado "−2.4 kg", sin pill ni %. Caso inverso al habitual "no inventar lo que la maqueta pide de más". | Se siguió la maqueta (texto plano `zinc-500`, el signo `−/+` ya comunica dirección) y se documentó que el % se quitó a propósito. La fidelidad a la maqueta también aplica cuando la maqueta es más austera que lo que ya existía; no aferrarse a la versión anterior. |
| Panel "Grasa / Masa magra" en el form de registro sin fuente en vivo | La maqueta de "Registrar medición" mostraba un panel inferior con "Grasa corporal estimada 18.7%" y "Masa magra 56.9 kg". Pero `bodyFatPercentage` y `leanMassKg` los calcula el **backend** (fórmula Marina EE.UU.) recién al guardar (`POST` los devuelve en la respuesta); el frontend no los tiene mientras se completa el form, y `agent.md §7` prohíbe calcular salud en el cliente. Mismo caso que "Marca/Nivel inventados". | El usuario eligió no renderizar el panel. `BodyMeasurementFormView` muestra solo los inputs; grasa/masa magra se ven en el dashboard de Salud después de guardar. No reimplementar la fórmula en el cliente para "completar" la maqueta. |
| Form de Salud todavía en `slate` light/dark | `BodyMeasurementFormView` y su pantalla `app/(tabs)/health/measurements.tsx` seguían en `bg-white dark:bg-slate-900` / `bg-slate-100 dark:bg-slate-950` con acento `rose-600 dark:rose-400`, mientras el resto de Salud ya es dark-only `zinc`/`rose-400`. | Se migró a `zinc` dark-only (color oscuro directo, sin prefijos `dark:`) y acento `rose-400`. CTA fijo dentro del scroll → CTA **flotante** con `TAB_BAR_HEIGHT` + `insets.bottom` porque la sub-pantalla vive en el Stack de `(tabs)/health` y el tab bar nativo se renderiza encima (mismo patrón que `FoodRegisterView`/`ExerciseDetailView`). |

| Fase de ejecución desacoplada del white-box compartido | La sesión activa (`ActiveSessionView`) renderizaba ejecución y descanso dentro de **un mismo white-box** con slide vertical (`exerciseBlockY`/`restBlockY`): `ExercisePhase` (en flujo normal) daba la altura y `RestPhase` se montaba **`position:absolute` + translateY** encima. La maqueta de ejecución pide una **pantalla completa dark**, incompatible con ese white-box. | Se desacoplaron las fases: `EXERCISE` ahora es una pantalla completa propia (`ExercisePhase` reescrito) y `REST` quedó **funcionalmente intacto** (timer/RPE/reps parciales/próximo ejercicio) pero se desacopló con un prop `slide?` en `RestPhase` (default `true`); en la sesión se pasa `slide={false}` para que fluya solo sin depender del bloque de ejercicio. Se perdió el slide-swap ejecución↔descanso a propósito (artefacto del white-box); la transición/diseño final del descanso se define cuando se rediseñe esa fase. |
| Maqueta de ejecución en azul vs. paleta real | Igual que el resto de la app, la maqueta de la ejecución de rutina venía 100% en azul (número de serie, `•`, timer, iconos de los botones). `colors.md` define Fitness = `lime-400`. | Todo el azul → `lime-400` (número de serie actual, `•` del subtítulo, timer e iconos de `SetActionButtons`). No copiar el azul literal. |
| Botones de sesión repetidos por fase | Ejecución usaba `ExerciseActionButtons` (3 botones) y descanso `RestActionButtons` (2 botones); la maqueta unifica en **una sección compartida** (2 botones grandes + "Finalizar rutina"). Copiar el bloque en cada fase sería el mismo error que `FilterPill`. | Se creó `SetActionButtons` (átomo de sesión) con labels/iconos/handlers por props, reusable en ambas fases. `ExerciseActionButtons` y `ExerciseStatsBar` quedaron **huérfanos** tras el rediseño (la stats-bar la reemplazan "Serie N de M" + subtítulo); `grep` confirmó cero consumidores → se borraron. `RestActionButtons` sigue vivo hasta rediseñar el descanso. |
| `SessionHeader` todavía en `slate` | El header de la sesión seguía en `border-slate-200 dark:border-slate-700` / `text-slate-900 dark:text-slate-50`, mientras el resto de la app ya es dark-only `zinc`. Su único consumidor es la sesión. | Migrado a `zinc` dark-only (color directo, sin prefijos `dark:`): `border-zinc-700`, `text-white`/`text-zinc-100`. Misma limpieza incremental de `slate` al tocar el archivo. |

| Maqueta de descanso mostraba pills de RPE, pero el usuario quiere el slider | La maqueta del descanso pintaba el esfuerzo percibido como una fila de pills 1–10 (con el 7 resaltado). El usuario aclaró que **se sigue usando el slider existente** (`SessionSlider` en `RpeSection`) y su funcionamiento (botón "Actualizar" → ajuste de carga), no las pills. | No se implementaron las pills: se mantuvo `RpeSection` (slider) tal cual a nivel de flujo, solo migrando el estilo `slate`→`zinc` y el título a la etiqueta uppercase de la maqueta. La maqueta no es ley cuando el usuario fija explícitamente el control a usar. |
| Sección de botones "estática" entre ejecución y descanso | El usuario pidió que la sección de botones NO cambie entre fases (mismo tamaño/posición); en descanso los labels pasan a "Finalizar rutina"/"Saltar descanso" y el enlace inferior "Finalizar rutina" **desaparece**, pero sin mover los dos botones. Quitar el enlace encogía la sección (anclada abajo) y subía/bajaba los botones. | `SetActionButtons` hace `onFinish` opcional: cuando falta, el enlace inferior se renderiza igual pero con `opacity: 0` + `pointerEvents: 'none'`, así **conserva exactamente su alto** y los dos botones quedan fijos. No usar render condicional que colapse el espacio. |
| `RestPhase` rediseñado: descacople total del white-box | El paso previo había dejado `RestPhase` desacoplado con un prop `slide`. Al rediseñar el descanso a pantalla completa (simétrico a `ExercisePhase`), `slide`/`restBlockY`/`exerciseBlockY`/`whiteBoxHeightRef` y el efecto de animación quedaron sin uso. | Se reescribió `RestPhase` como pantalla completa propia y `ActiveSessionView` quedó como un simple switch de fases. Se **limpió el hook** `useActiveSession` (se borraron los shared values, el ref y el efecto de animación + el import `useSharedValue/withTiming`). `TimerCard` y `RestActionButtons` quedaron huérfanos (los reemplazan el timer+`FillBar` y `SetActionButtons`) → `grep` confirmó cero consumidores → borrados. (`RepetitionSection` ya era código muerto preexistente; se dejó, fuera de scope.) |
| Barra del timer de descanso: reuso de `FillBar` en vez de `TimerCard` | El timer viejo (`TimerCard`) traía su propio texto 5xl italic + barra animada de 2 segmentos, incompatible con el timer grande `lime` de la maqueta. El usuario dejó a criterio dónde poner "la barrita". | El timer grande se pinta directo (`text-8xl lime`, `adjustsFontSizeToFit`) y la barra se resolvió reusando el átomo `FillBar` (`accent="lime"`, `progress = restTimeLeft/initialRest`) justo debajo. Se pierde la animación suave entre segundos (la barra ahora "salta" por segundo), aceptable para un descanso y evita mantener `TimerCard`. |

| Resumen post-sesión: maqueta pedía datos que el hook de sesión no tiene | La maqueta del resumen muestra el **nombre de la rutina** ("Fuerza Pro") y la **próxima sesión**, pero `useActiveSession` solo recibe `routineId` + `day` (`SessionDay`), no el `Routine` completo ni el día siguiente. Inventarlos sería el mismo error que "Marca/Nivel inventados". | Se pasaron `routineName` y `nextSessionDay` por la **cadena de navegación** (`RoutineDetailView`/`RoutineDetailModal` → params `/session` → `session.tsx` → `ActiveSessionView` → `SummaryPhase`), donde sí existe `routine.name` y `sortedDays`. El próximo día se calcula `sortedDays[(activeDayIndex + 1) % length]` (con wrap; si la rutina tiene 1 día, repite). Props opcionales: si faltan, esas filas no se renderizan. |
| "Próxima sesión: Martes • Espalda" sin día de semana real | La maqueta dibuja un día de semana ("Martes") + grupo ("Espalda"), pero el modelo NO tiene día de semana: `RoutineDay.day` es `"Día 2 - Espalda"`. El usuario pidió explícitamente sacar el grupo y el chevron, "que solo diga el día de la siguiente sesión". | `formatNextSessionDay` corta la etiqueta antes del `-` → muestra solo `"Día 2"`. No se inventó "Martes" (no hay weekday en el DTO). Mismo criterio honesto que "Próxima sesión" del resumen. |
| Resumen rediseñado MÁS austero: se quitó el detalle por ejercicio | El `SummaryPhase` viejo listaba sets/reps/estado por cada ejercicio; la maqueta nueva NO lo tiene (solo grid de stats + próxima sesión). Caso "maqueta más simple que la UI actual" (igual que `BodyMetricTrendCard` en Salud). | Se siguió la maqueta y se quitó el detalle por ejercicio (junto con las props `logs`/`exercises` que `SummaryPhase` ya no necesita). La fidelidad a la maqueta también manda cuando es más austera que lo que existía. |
| Resumen: tiempo con horas vs `formatTime` MM:SS | La maqueta muestra `01:16:25` (HH:MM:SS), pero el resumen viejo usaba `formatTime` que devuelve solo `MM:SS`. Una sesión de +1h se vería mal. | Se usó `formatTimeSpan` (HH:MM:SS) en el resumen. Verificar el formatter correcto, no asumir que "el que ya estaba" cubre el caso de la maqueta. |
| Transición de fase: animar solo el contenido, no el chrome | Un cross-fade de pantalla completa entre ejecución y descanso también desvanecía el header y la sección de botones, que conceptualmente son "estáticos" (no deben parpadear). | Se subió el scaffold (`SessionHeader` + `SetActionButtons`) a `ActiveSessionView` y `ExercisePhase`/`RestPhase` quedaron como **solo contenido**. El header y los botones quedan fijos (re-render instantáneo de labels/acción por fase) y solo el contenido central se envuelve en `Animated.View key={phase}` con `FadeInDown`/`FadeOut`. Patrón general: para transicionar entre vistas que comparten chrome, animar únicamente la zona que cambia, no toda la pantalla. |

| Maqueta de Perfil en azul + sin acento de módulo | La maqueta del Perfil mostraba "Premium" y detalles en azul, pero Perfil no es un módulo (no tiene `lime`/`amber`/`rose`). `colors.md` define Perfil = "resto de la UI" → `zinc` neutro, acento global dark = `zinc-50`. | No se inventó un acento: la pantalla quedó 100% `zinc` mono. "Premium" es un badge sutil (pill `border-zinc-700 bg-zinc-900 text-zinc-200`), no texto azul. Misma regla que "maqueta en azul vs paleta real" pero sin módulo al cual traducir. |
| Maqueta de Perfil con filas SIN íconos | A diferencia de Fitness/Nutrición/Salud, las filas de la lista de Perfil en la maqueta son texto puro (sin tile de ícono). Se evaluó agregar íconos con acento de módulo, pero contradecía la maqueta y Perfil es neutro. | Se siguió la maqueta: `ProfileListRow` es solo `label` + chevron/valor, sin ícono. Fidelidad a la maqueta aunque sea más austera que otras pantallas (mismo criterio que "maqueta más simple que la UI actual" en Salud). |
| Sub-vistas de Perfil sin header propio dependen del nav bar del padre | `FitnessSettingsView`/`HealthSettingsView` NO renderizan su propio header: solo una `ScrollView` de items, y reportan su back de sub-vista vía `onSubBackChange` al **nav bar compartido** de `profile.tsx`. `DataManagement` sí trae `BackButton` propio (inconsistente). | Al rediseñar la lista principal se **conservó el nav bar compartido** (back + título de sección) cuando `activeSection !== null`; solo la lista raíz (`activeSection === null`) usa el diseño nuevo. Borrar el nav bar habría roto el back de Fitness/Salud. Verificar si una sub-vista trae header propio ANTES de cambiar el contenedor que la monta. |
| Perfil es ruta pusheada, no tab (pese a la maqueta) | La maqueta dibuja "Perfil" en el tab bar, pero en la app real `app/profile.tsx` se pushea desde el home (`GreetingHeader` → `router.push('/profile')`) y está registrada en el `_layout.tsx` raíz **fuera de `(tabs)`**. | No hay tab bar nativo en esta pantalla → el `paddingBottom` del scroll usa solo `insets.bottom` (sin `TAB_BAR_HEIGHT`) y se mantuvo un botón back (la maqueta no lo muestra, pero es una pantalla pusheada). No convertir a tab: el pedido era rediseñar la vista, no reestructurar la navegación. |
| Filas de menú de Perfil duplicadas (3 variantes paralelas) | Existían `MenuItem` + `LogoutItem` (en `ProfileMenuPanel`) y `ConfigMenuItem` como tres filas casi idénticas — el mismo error que `FilterPill`/`SelectablePill`. | Se atomizó en `ProfileListRow` (con `destructive` y `value` para cubrir logout y versión) + `ProfileListGroup`. `ProfileMenuPanel` quedó huérfano tras el rediseño → `grep` confirmó cero consumidores activos (solo doc) → borrado. `ProfileHeader`/`SettingsSection`/`ConfigMenuItem` ya eran muertos (solo `_profile_old.tsx.bak`) → se dejaron fuera de scope. |

| Sub-vistas de Perfil con doble header tras agregar el nav bar compartido | `DataManagement` y `DietaryConfig` traían su **propio** `BackButton` (cyan) + título; el resto de sub-vistas dependía del nav bar compartido. Al rediseñar `profile.tsx` con nav bar compartido, esas dos quedaban con doble back. | Se les quitó el `BackButton` propio y el back pasó al nav bar compartido. Para no perder el back al submenú de Nutrición, se agregó `onRegisterBackHandler` a `DietaryConfig` y `onSubBackChange` a `NutritionSettingsView` (mismo patrón que Fitness/Salud). Antes de unificar headers, mapear qué sub-vista trae header propio y cuál no. |
| Submenús de Perfil con `SubMenuItem` triplicado | `FitnessSettingsView`/`NutritionSettingsView`/`HealthSettingsView` tenían cada una su propio `SubMenuItem` (con tile de ícono `slate`), 3 copias casi idénticas — el mismo error que `FilterPill`. | Los 3 pasaron a reusar `ProfileListGroup` + `ProfileListRow` (sin íconos, consistente con la maqueta de Perfil) y se borraron los 3 `SubMenuItem`. Reutilizar los átomos nuevos en vez de mantener filas paralelas por módulo. |
| Migración `slate`→`zinc` de Perfil: hasta dónde llega el blast radius | Las `*Config` del perfil estaban en `slate`+cyan, pero usan átomos compartidos con onboarding (`SectionCard`, `CheckableCard`, `SearchableSelect`, `TagSelect`, etc.). Migrar todo arrastraba onboarding. | Se migraron solo los estilos **propios** de cada `*Config` (cards inline, bottom bars, CTAs cyan→`bg-zinc-50` mono, spinners `#64748b`→`#a1a1aa`) y se dejaron los átomos compartidos en `slate` como deuda (`slate-900≈zinc-900`, diferencia mínima). Limpiar slate incrementalmente sin invadir onboarding en un cambio de Perfil. |
| Premisa de plan PWA sin verificar contra `node_modules` (tokenCache "bloqueante" que no lo era) | `docs/pwa-web-support-plan.md` diagnosticaba `expo-secure-store` como 🔴 BLOQUEANTE en web: el stub web (`export default {}`) haría lanzar `getItemAsync` y por eso "la sesión no persiste tras recargar". Al leer `node_modules/@clerk/clerk-expo/dist/provider/ClerkProvider.js:64-70`, en web (`isNative() === false`) Clerk se instancia con `Clerk: null` + `standardBrowser: true` y el `tokenCache` **solo se pasa a la instancia nativa** — en web Clerk directamente lo ignora; la sesión la persiste `clerk-js` por cookies/localStorage, no por el tokenCache. Además `clerk-expo` ya trae un `tokenCache` oficial multiplataforma en `@clerk/clerk-expo/token-cache` (`isNative() ? createTokenCache() : void 0`, doc dice explícito "undefined on web"). | Antes de aceptar el diagnóstico de un plan/doc sobre por qué algo "rompe", verificar el **consumo real** de la API en `node_modules` (no solo si la API existe/lanza). El fix no fue crear `token-cache.ts`/`token-cache.web.ts` a mano (lo que pedía el plan): se reemplazó el objeto inline de `app/_layout.tsx` por el `tokenCache` oficial de `@clerk/clerk-expo/token-cache` — mismo comportamiento nativo (SecureStore), `undefined` en web (que es lo que Clerk ya esperaba), cero archivos nuevos, sin reinventar algo que el SDK ya resuelve. |

| Re-rediseño de Perfil: bullets de la maqueta ≠ sub-secciones reales | La nueva maqueta de Perfil dibuja cards de módulo con bullets (Fitness: Objetivos/Experiencia/Equipamiento; Nutrición: Objetivos/Calorías/Preferencias; Salud: Mediciones/Lesiones/Condiciones), pero el config real de cada módulo tiene OTROS labels y "Mediciones" ni siquiera está en el config de Salud (vive en el tab Salud). Inventar los bullets sería el mismo error que "Marca/Nivel inventados". | `ProfileModuleCard` recibe los **labels reales** del settings view de cada módulo (Fitness: Equipamiento/Días y duración/Sub objetivo; Nutrición: Sub objetivo/Alergias alimenticias/Estilo de dieta; Salud: Lesiones/Afecciones médicas). Verificar el contenido real de `FitnessSettingsView`/etc. antes de copiar los bullets de la maqueta. |
| Re-rediseño de Perfil: maqueta otra vez en azul + Perfil sigue neutro | La nueva maqueta de Perfil volvió 100% azul (badge Premium, icon-tiles de módulo, bullets, flechas, botón "Cerrar sesión"). Perfil no es módulo (no tiene `lime`/`amber`/`rose`); `colors.md` → "resto de la UI" = `zinc` neutro. Se consultó al usuario si las cards de módulo debían usar el acento de cada módulo o quedar neutras. | El usuario eligió **zinc neutro mono** (consistente con la decisión previa de Perfil). Todo el azul → `zinc`: icon-tiles `IconTile` con `color #d4d4d8`, badge Premium con `diamond-outline` zinc, "Cerrar sesión" como botón outline. **Update posterior**: el usuario pidió que sea **rojo** (`border-red-500/50`, ícono/texto `red-400`) como acción destructiva — aunque la maqueta lo pinte azul, el usuario priorizó el cue de peligro. No copiar el azul literal. |
| Swipe-back nativo popea toda la ruta en sub-navegación por estado | `app/profile.tsx` navegaba sus sub-secciones (Fitness/Nutrición/Salud/Configuración) por **estado** (`activeSection`), no por rutas reales. El **gesto swipe nativo de iOS** popeaba toda la ruta `/profile` → volvía al home. **Primer intento**: `usePreventRemove(activeSection !== null, cb)` (RN Navigation v7). NO sirvió: con native-stack el gesto es nativo, arranca la animación de salida y el JS la cancela → **glitch visible** ("se va al home y salta de vuelta a Perfil"). `preventNativeDismiss` no cancela el gesto de forma suave. | **Solución correcta (la que quedó)**: convertir las secciones en **rutas reales** (`app/profile/_layout.tsx` Stack anidado + `index` + `fitness`/`nutrition`/`health`/`settings`). El swipe vuelve a la vista anterior con animación nativa, sin glitch. El scaffold `ProfileSectionScreen` centraliza nav bar + back interno de los configs anidados (que siguen por estado); mientras un config está abierto **se deshabilita el gesto** (`navigation.setOptions({ gestureEnabled: false })`) para evitar el glitch en ese nivel, y `usePreventRemove` cubre el back físico de Android sin glitch. Los 7 `*Config` (con back interno multi-paso vía `backHandlerRef`/`onRegisterBackHandler`) quedaron intactos: route-ificar su back interno era un refactor mayor que el bug no requería. Lección: `usePreventRemove` NO arregla el gesto nativo de iOS de forma suave; para swipe-back real entre vistas hay que usar rutas reales. |
| Typed-routes de expo-router corruptos tras mover archivos | Al pasar `app/profile.tsx` → `app/profile/index.tsx` + rutas, `tsc` falló con `'/profile' is not assignable` en `GreetingHeader` (`router.push('/profile')`). El `.expo/types/router.d.ts` (typed routes) había quedado **corrupto** por una regeneración a mitad de camino del metro watcher: listaba `/profile/index` en vez de `/profile` e incluía componentes de `src/` como si fueran rutas (`/../src/components/.../ProfileModuleCard`). `expo export` compila pero **no** regenera ese archivo. | Se regeneró limpio: `rm .expo/types/router.d.ts` + `expo start --port <libre>` unos segundos (el watcher lo reescribe al arrancar; había un metro ya ocupando 8081). Tras regenerar, aparece `/profile` (no `/profile/index`), cero entradas `src/`, y `tsc` da 0 errores. No "arreglar" el síntoma cambiando el `router.push` a `/profile/index`: el archivo generado estaba corrupto, no el código. |
| Perfil pasó de lista de texto a cards con icon-tile (vuelta atrás de diseño) | El Perfil anterior era lista agrupada plana de texto puro (`ProfileListRow`/`ProfileListGroup`) "porque la maqueta no tenía íconos". La nueva maqueta SÍ trae icon-tiles + bullets. `ProfileListRow`/`ProfileListGroup` quedaban huérfanos en la raíz, pero **siguen usándose en las 3 sub-vistas** (`FitnessSettingsView`/`NutritionSettingsView`/`HealthSettingsView`). | Se creó `ProfileModuleCard` (reusa `IconTile`) para la raíz y se **conservaron** `ProfileListRow`/`ProfileListGroup` (no borrar: `grep` confirma uso en las sub-vistas). No asumir que un átomo desplazado en una pantalla está huérfano sin chequear el resto. `ProfileIdentityHeader` se restyleó de bloque centrado a card horizontal y dejó de renderizar el email (fidelidad a la maqueta), manteniendo el prop por compatibilidad. |
| Sub-menús de Perfil desentonaban con la raíz rediseñada | Tras pasar la raíz a `ProfileModuleCard` (cards redondeadas con icon-tile + chevron), las 3 sub-vistas (`FitnessSettingsView`/`NutritionSettingsView`/`HealthSettingsView`) seguían en lista plana de texto (`ProfileListGroup` + `ProfileListRow`) sin íconos → chocaban con el lenguaje visual nuevo. | Las 3 sub-vistas pasaron a reusar **`ProfileModuleCard`** (el mismo átomo de la raíz; ya soporta "solo título" sin `subtitle`/`bullets`) en vez de inventar una card de sub-item nueva (evita el error tipo `FilterPill`). Cada item lleva un ícono **decorativo de UI** (no dato del backend, así que elegirlo no viola "no inventar"). `ProfileListGroup`/`ProfileListRow` quedaron huérfanos (`grep` confirmó cero consumidores `.tsx`) → borrados. Reusar el átomo de navegación existente del Perfil para cualquier fila/card, raíz o sub-vista. |
| `gestureEnabled:false` NO frena el gesto nativo de iOS de forma confiable | La lección #112 route-ificó el nivel **sección** del Perfil pero dejó los **configs** navegando por estado, confiando en `navigation.setOptions({ gestureEnabled:false })` (vía un `useEffect`) para desactivar el swipe mientras un config estaba abierto. En la práctica ese toggle NO frenaba el gesto, mientras que `usePreventRemove` (un hook) SÍ se activaba → asimetría: el swipe arrancaba la salida nativa (se veía el home) y el hook la cancelaba de un salto. El usuario reportó exactamente ese glitch al volver de un config. | **Rutas reales también para los configs.** Cada config pasó a ser ruta real (`/profile/fitness-equipment`, etc.) dentro del mismo stack de Perfil; el swipe-back es 100% nativo y suave en todos los niveles. Se borró toda la plomería de estado (`onSubBackChange`/`backHandlerRef`/`onRegisterBackHandler`/`gestureEnabled`) y el guard de "cambios sin guardar" se atomizó en el hook `useUnsavedChangesGuard` (`usePreventRemove` SOLO cuando hay cambios reales — el glitch residual queda acotado a ese caso, comportamiento estándar de RN). Lección general: para back/swipe suave entre vistas, **rutas reales**; no confiar en `gestureEnabled` dinámico ni en `usePreventRemove` para simular navegación por estado. |

| Biblioteca "Mis rutinas": filtro de source vs. fecha sin inventar UI | La maqueta de "Mis rutinas" solo dibuja el toggle Todas/IA/Manual (azul) y no muestra filtro de fecha, pero el usuario pidió agregar el filtro por fecha "de la mejor forma que combine". El `RoutineFilters` viejo era un panel colapsable `slate` con source + fecha juntos. | Source → `SegmentedControl` (Todas/IA/Manual, `accent="lime"`) full-width como la maqueta; fecha → botón calendario al lado que despliega `SelectablePill` (Cualquier fecha/Última semana/Último mes, `accent="lime"`). Se reusaron átomos existentes en vez de inventar un control nuevo. El azul de la maqueta → `lime-400` (misma regla de siempre). `RoutineFilters` quedó **huérfano** (solo lo usaba `routines.tsx`) → `grep` confirmó cero consumidores → borrado. |

| PWA Fase 2: el plan asumió fallback silencioso a `MemoryVFS` en web | `docs/pwa-web-support-plan.md` diagnosticaba que sin OPFS, `expo-sqlite` web "cae a `MemoryVFS` (no persiste)". Verificado en `node_modules/expo-sqlite/web/worker.ts:405` + `web/wa-sqlite/AccessHandlePoolVFS.js:218-235`: una DB con **nombre de archivo** (nuestro caso, `wellium-offline.db`) usa SIEMPRE el VFS persistente `AccessHandlePoolVFS`; `MemoryVFS` es solo para paths `:memory:`. Sin OPFS, `maybeInitAsync` **lanza** `Error('Failed to initialize AccessHandlePoolVFS')` — no degrada solo. | Se agregó detección propia (`src/offline/storage-support.ts` nativo=`true` / `storage-support.web.ts` chequea `navigator.storage.getDirectory` + `isSecureContext`) y `getOfflineDb` (`db.ts`) lanza un error tipado (`OfflineStorageUnavailableError`) **antes** de llegar al throw críptico del VFS, para que los consumidores degraden a network-only de forma controlada. No confiar en un fallback de una librería sin verificarlo en el código fuente real. |
| PWA Fase 2: WAL sobre `AccessHandlePoolVFS` sin shared-memory | `AccessHandlePoolVFS` (backend web de expo-sqlite) no provee shared-memory para el WAL-index que `PRAGMA journal_mode = WAL` necesita; correrlo tal cual en web podía romper las migraciones (`db.ts`) sin previo aviso. | Se agregó guard `Platform.OS !== 'web'` alrededor del `PRAGMA journal_mode = WAL` en `runMigrations` (`db.ts`); web queda en el journal `delete` por defecto (compatible con el VFS). El path nativo no cambia. Pendiente validar empíricamente si el guard era estrictamente necesario (tiraba error) o defensivo. |
| PWA Fase 2: `wasm` no resuelve por defecto en Metro | El worker de `expo-sqlite` (`web/worker.ts:22`) hace `import wasmModule from './wa-sqlite/wa-sqlite.wasm'`, pero `wasm` no está en `assetExts` por defecto de Metro ni de `@expo/metro-config` (0 coincidencias verificadas). Sin el fix, el bundling web del worker rompería en build/runtime. | Se agregó `"wasm"` a `config.resolver.assetExts` en `metro.config.js`, junto al manejo existente de `svg`. Inerte en nativo: el único `import` de `.wasm` del repo vive en `expo-sqlite/web/`, código que el bundle nativo nunca toca. |
| 🔴→✅ BLOQUEANTE nuevo (no de Fase 2, RESUELTO): la app web no renderizaba NADA | Al validar Fase 2 con `expo export -p web` y con `expo start --web` (dev server), **ambos** fallaban en el `root` antes de cualquier ruta: `TypeError: Appearance.default.setColorScheme is not a function` en `app/_layout.tsx:34` (`Appearance.setColorScheme('dark')`, código preexistente de Fase 1). Verificado en `node_modules/react-native-web/src/exports/Appearance/index.js`: esa versión (`0.21`) solo expone `getColorScheme` y `addChangeListener` — **no** tiene `setColorScheme`. Con `web.output: "static"`, expo-router pre-renderiza (SSR) el layout raíz, así que el throw tumbaba **toda** la app. Además, aunque no hubiera lanzado, la llamada **nunca forzaba dark en web**: `useRNColorScheme()` en `react-native-web` lee `prefers-color-scheme` vía `matchMedia`, sin conexión con `Appearance.setColorScheme`. | Dos cambios, reusando el patrón `.web.ts` ya existente en el repo (`use-color-scheme.web.ts`): (1) `app/_layout.tsx` guarda la llamada bajo `Platform.OS !== 'web'` (nativo sin cambios). (2) `use-color-scheme.web.ts` fuerza `'dark'` directo tras hidratar (en vez de confiar en `useRNColorScheme()`/system preference), preservando el `'light'` pre-hidratación para SSR-safety que el hook ya tenía. Validado con `expo start --web`: bundling sin errores, `curl` al root sin `_expo-static-error`, `#root` con markup real. `tsc --noEmit` y ESLint limpios. |
| `RoutineListCard` rediseñada con dos variantes en un mismo componente | La maqueta de "Mis rutinas" tiene la rutina activa como card-hero (borde lime + CTA "Continuar rutina") y el resto como filas compactas. Tentación de hacer 2 componentes paralelos (error tipo `FilterPill`). | Una sola `RoutineListCard` ramifica por `routine.isActive` (hero vs. fila), reusando `IconTile`. La activa se ordena primero en la pantalla (`sort` por `isActive`). El CTA "Continuar rutina" abre el detalle (mismo `onPress` que el tap), consistente con el "Continuar rutina" de `ActionCard` — no se inventó navegación a sesión. |
| "Crear rutina" en una sub-ruta cuyo overlay vive en otra pantalla | La maqueta pone "Crear rutina" en "Mis rutinas" (`fitness/routines.tsx`), pero el overlay de creación (`CreateRoutineView` + medición de card + contexto) vive entero en `fitness/index.tsx`. Llamar `onCreateRoutine` del contexto desde `routines.tsx` montaría el overlay en la pantalla index (abajo, invisible). | Se navega al index con un param (`router.push({ pathname:'/fitness', params:{ openCreate:'1' }})`) y `index.tsx` abre el creador en un effect que limpia el param, **reusando el patrón ya existente de `openRoutineId`**. No se duplicó el overlay ni el flujo de creación. |
| CTA flotante de "Mis rutinas" sobre el tab bar nativo | `fitness/routines.tsx` vive en `(tabs)` con stack `headerShown:false` → el tab bar nativo se renderiza encima. Un CTA `bottom-0` quedaría tapado. | CTA `absolute` a `bottom: insets.bottom + TAB_BAR_HEIGHT + 8` y el `paddingBottom` del `FlatList` suma ese offset (+88 para el alto del botón). Mismo patrón ya documentado para `FoodRegisterView`/`ExerciseDetailView`/form de Salud. Se migró la pantalla de `DarkSheetLayout` (slate) a `View bg-zinc-950` dark-only de paso. |

| Historial replicó "Mis rutinas" pero sin filtro de source | El usuario pidió el mismo diseño que "Mis rutinas" para el historial (`fitness/training-history`), pero sin toggle IA/Manual (todas las sesiones son iguales), solo filtro de fecha. Copiar el `SegmentedControl` + botón calendario habría dejado un control vacío. | Se omitió el `SegmentedControl` y, como el filtro de fecha es la única dimensión, las `SelectablePill` de fecha (Todas/Hoy/Última semana/Último mes, `accent="lime"`) van **siempre visibles** (no detrás de un toggle como en "Mis rutinas"). Mismo lenguaje visual (header grande + conteo, fila compacta `IconTile`, `bg-zinc-950`), adaptado a una sola dimensión de filtro. La pantalla migró de `DarkSheetLayout` (slate) a dark-only `zinc`. |
| `useTrainingHistoryList` filtra contra el server (no client-side) → stale closure | A diferencia de `useMyRoutines` (filtra en cliente y auto-aplica con un effect), `useTrainingHistoryList.applyFilters` hace `loadFirstPage(filters)` (refetch). Hacer `setDateRange(...)` + `applyFilters()` en el mismo handler del pill usaba el `filters` viejo (setState async) → filtraba con el rango anterior. | Se agregó `applyDateRange(from, to)` al hook: arma el `next` explícito, hace `setFilters(next)` y `loadFirstPage(next)` con ese valor, sin depender del state recién seteado. Los pills llaman `applyDateRange` directo. No se metió un effect auto-aplicador para no disparar refetches dobles con `refresh()`. |
| Borrado de huérfanos del historial sin tocar lo compartido | Al rediseñar, `TrainingHistoryFilters` (panel slate colapsable) y `EmptyTrainingHistory` quedaron huérfanos (solo los usaba la pantalla; el empty se inlineó como en `routines.tsx`). PERO `TrainingHistoryListCard` parecía huérfano y **no lo es**: lo usa `SessionComparePicker`; y `TrainingHistoryCardSkeleton` es **compartido** con el detalle (`[id].tsx`). | `grep` antes de borrar: se borraron solo `TrainingHistoryFilters` + `EmptyTrainingHistory`. `TrainingHistoryListCard` se dejó intacto (en uso por el compare picker — misma lección que "no asumir huérfano sin chequear el resto"). El skeleton se **restyleó** a `zinc` (no se borró) porque lo comparte el detalle. Ojo con la colisión de nombres: el tipo `TrainingHistoryFilters` (interfaz de filtros en `types/`) ≠ el componente borrado. |

| Detalle de sesión: stats de la maqueta sin endpoint de resumen | La maqueta del detalle pedía "Series completadas 11/12", "Repeticiones 132" y "Esfuerzo promedio RPE 5", pero `TrainingHistorySession` no trae un objeto resumen: solo `exercises[].sets[]` (`isCompleted`, `repsPerformed`, `weightUsed`) y `exercises[].rpe`. Riesgo de inventar un DTO o, al revés, de no implementar las stats por miedo a `agent.md §7`. | Se computaron en cliente con `computeSessionStats` (sumar sets completados/total, sumar `repsPerformed`, promediar `rpe>0`). **Esto es agregación de presentación, NO un cálculo de salud**: `agent.md §7` prohíbe TDEE/IMC/grasa corporal (fórmulas de dominio), no contar/sumar datos que el backend ya devolvió. Distinguir ambos casos antes de bloquear una stat. |
| Detalle de sesión: tabla de columnas → filas "Serie N" | El `SetsTable` viejo era una tabla densa (`# Reps Peso Dur ✓`); la maqueta usa filas simples `Serie N · 12 rep • 20 kg`. Se reescribió en vez de crear un componente paralelo (solo lo usa `SessionExerciseCard`). El set incompleto muestra `—` (`text-zinc-600`), derivado de `isCompleted` real, no inventado. Se quitaron columnas Duración/✓ y los `targetMuscles` de la card (maqueta más austera, mismo criterio que Salud/Resumen). La pantalla migró de `DarkSheetLayout` (slate) a `View bg-zinc-950` dark-only, con `paddingBottom` = `TAB_BAR_HEIGHT + insets.bottom` por el tab bar nativo. |

| Comparación de sesiones sin orden cronológico | La maqueta de "Comparación de sesiones" asume comparación **vieja → nueva** ("Mejor que la sesión anterior", columna target resaltada = la más reciente), pero `buildTrainingSessionComparison(base, target)` hacía `target - base` según quién abrió el detalle vs. quién se eligió en el picker — no según fechas. Un usuario podía ver una sesión vieja y comparar contra una nueva (o al revés), invirtiendo el sentido del diff y rompiendo el veredicto. | El util ahora **ordena por `trainedAt`**: `base` = más antigua, `target` = más reciente, así el diff siempre representa "anterior → actual". El header muestra `target ↔ base` (más nueva primero, como la maqueta) y la tabla `base → target` (cronológico). No asumir que el orden de los argumentos refleja el orden temporal. |
| "Rendimiento general" y top set por ejercicio: derivar sin inventar | La maqueta pedía un veredicto general ("Mejor que la sesión anterior"), mini-stats firmados (+2 series, +38 s) y por ejercicio "20 kg × 12 → 25 kg × 12 / +5 kg". Nada de eso viene del backend como objeto. Riesgo: inventar un DTO de resumen, o no implementarlo por miedo a `agent.md §7`. | Todo se computa en cliente desde `sets[]` reales: top set = set completado de mayor peso (desempate por reps), headline = `+kg` si cambió el peso, `+repeticiones` si cambió solo las reps; veredicto = balance de series completadas + volumen. **Es agregación de presentación, NO cálculo de salud** (igual que `computeSessionStats`): `agent.md §7` prohíbe TDEE/IMC/grasa, no contar/derivar sobre datos que el backend ya devolvió. Se quitaron del util la fila "Ejercicios" (conteo) y las métricas densas por ejercicio (volume/maxWeight/rpe) que la maqueta no muestra (más austera). |
| Maqueta de comparación en azul + verde vs. paleta real | La maqueta usaba **azul** para la sesión actual/target y los stats neutros, y **verde** para las mejoras (+5 kg, "Mejor..."). `colors.md` → Fitness = `lime-400`, sin verde semántico definido. | Todo el azul **y** el verde → `lime-400` (acento único del módulo, ya era el patrón del `SessionComparisonSheet` viejo: positivo=lime, neutro/negativo=zinc). No introducir un verde semántico aparte; `lime` cubre "actual" y "mejora", `zinc-500` cubre "sin cambios"/regresión. Misma regla "azul → acento del módulo" de siempre. |
| Comparación full-screen en la maqueta, sheet en la app | La maqueta dibuja la comparación como **pantalla completa** (status bar, back circular), pero en la app es un `BottomSheetModal` montado condicionalmente en `[id].tsx` (`comparisonSheetVisible`). Convertirlo a ruta era un refactor de navegación fuera de scope. | Se mantuvo el `BottomSheetModal` (`height 88%` para dar sensación full) y se replicó el header de la maqueta **dentro** del sheet: back circular `lime` que llama `onClose`. Mismo criterio que Perfil ("rediseñar la vista, no reestructurar la navegación"). |

| Detalle de medición todavía en `slate` light/dark | `MeasurementDetailView` y el loader de su pantalla (`app/(tabs)/health/[id].tsx`) seguían en `bg-white/bg-slate-* dark:slate-*` con acento `rose-600`, mientras el resto de Salud ya es dark-only `zinc`/`rose-400`. | Migrado a `zinc` dark-only (color directo, sin prefijos `dark:`) y acento `rose-400` (`#fb7185`). El loader pasó de `#e11d48` a `#fb7185`. Misma limpieza incremental de `slate` al tocar el archivo. |
| Columnas de composición duplicadas (dashboard + detalle) | Las columnas Peso/Grasa/Masa magra con divisores verticales vivían inline en `LastMeasurementCard`; el detalle rediseñado las repetía → mismo error que `FilterPill`/`IconTile`. | Se atomizó en `health/BodyCompositionColumns` (recibe `items[]`, cada consumidor arma sus labels/formato). La consumen `LastMeasurementCard` y `MeasurementDetailView`. Antes de repetir el `flex-row` con columna + divisor, atomizar. |
| Detalle de medición: maqueta sólo muestra la hora + leader punteado | La maqueta del detalle muestra "Registrado a las 18:33" (solo hora, no fecha completa como antes) y filas perimetrales con `label · · · · valor` unidas por una línea punteada. | Nuevo `formatCapturedTime` (solo `HH:MM`) y leader con `border-b border-dotted border-zinc-700 flex-1` entre label y valor. El azul de la maqueta (ícono calendario, chevron) → `rose-400` (regla de siempre). CTA "Comparar" pasó de pill sólido `rose` a outline (`border-zinc-700`) como la maqueta, con ícono `analytics-outline` rose. |

| Comparación de mediciones sin orden cronológico | Igual que la comparación de sesiones (#128), `buildMeasurementComparison(base, target)` hacía `target - base` según qué registro se abrió primero vs. cuál se eligió en el picker — no según fechas. Abrir una medición vieja y comparar contra una nueva invertía el sentido del diff (la maqueta asume **anterior → actual**: 85 → 70 = -15). | El util ahora **ordena por `date`** al entrar (`base` = más antigua, `target` = más reciente; las fechas `YYYY-MM-DD` permiten comparar strings). El header del sheet muestra `target vs base` (más reciente primero, como la maqueta) y la lista fluye `base → target`. Mismo criterio que el de sesiones; no asumir que el orden de los argumentos refleja el orden temporal. |
| Maqueta de comparación de mediciones en verde/rojo vs. paleta real | La maqueta pintaba las bajadas en **verde** y las subidas en **rojo**. Choca con `colors.md` (Salud = `rose-400`, sin verde semántico), con `BodyMetricTrendCard` (cambio en texto plano `zinc`, solo signo) y con `agent.md §7` + el comentario del propio util ("no interpreta si el cambio es positivo o negativo para la salud"). Pintar "-9.9 kg de masa magra" en verde implicaría que perder músculo es bueno (engañoso). | Se **consultó al usuario** y eligió **verde/rojo como la maqueta**, por fidelidad visual. Se implementó como color **por DIRECCIÓN** (baja = `green-400`, sube = `red-400`, igual = `zinc-500`), NO como interpretación de salud: el frontend no decide "bueno/malo", solo refleja el signo del diff que ya calculó. Excepción puntual a la regla "azul/verde de la maqueta → acento del módulo": acá el usuario priorizó el cue de dirección. Los valores `nuevo` siguen en `rose-400` (acento del módulo). |

| Login en `slate` + maqueta en azul + paginador de 2 puntos | El login viejo (`app/login.tsx`) estaba en `slate` light/dark con dos círculos de marca. La maqueta nueva venía 100% en azul y mostraba un dot indicator de 2 puntos ("1 de 2"), implicando un carrusel de bienvenida de 2 slides — pero solo había diseño de UNA. Inventar la 2.ª slide sería el mismo error que "Marca/Nivel inventados". | Se migró a dark-only `zinc` (la app fuerza dark; sin variantes light). **Color:** login NO es módulo (`colors.md` → "resto de la UI" = `zinc` neutro); se consultó al usuario y eligió **todo zinc neutro** (mismo criterio que Perfil), así que las 3 palabras del título van en blanco y los íconos en zinc-300 (no se tradujo el azul a lime/amber/rose). **Paginador:** se consultó y el usuario eligió **pantalla única sin paginador** (no se dibujó el dot muerto ni se inventó la 2.ª slide). Íconos de features = los mismos del tab bar (`barbell`/`restaurant`/`heart`-outline) por consistencia. La línea de Términos/Privacidad se excluyó por pedido del usuario. |
| Gradiente sobre texto sin desentonar de la paleta neutra | El usuario pidió un gradiente en "Evoluciona." que "pegue y no desentone". La maqueta lo tenía violeta→azul, pero la app es `zinc` neutra y ya se había decidido login neutro. Meter color de marca rompería esa decisión. Además, gradiente sobre texto en RN no sale con `Text` solo: necesita `MaskedView` + `LinearGradient`. | Se atomizó `common/GradientText` (`MaskedView` forma + `LinearGradient` color + `Text` invisible para dimensionar). Se instaló `@react-native-masked-view/masked-view` vía `expo install` (parte del SDK → corre en Expo Go, no requiere prebuild, a diferencia de `@expo/ui`). El gradiente default del átomo es **plata neutro** (`zinc-50 → zinc-300 → zinc-500`). **Primer intento:** plata en el login → el usuario lo vio muy sutil/poco llamativo. **Resuelto:** se pasó por prop `colors` un gradiente **violeta→índigo→azul** (`#c4b5fd → #818cf8 → #3b82f6`, casi horizontal) — el de la maqueta original. Sobre `zinc-950` resalta sin recargar y es el único punto de color de la pantalla (acento puntual en el clímax del título, el resto sigue neutro). Lección: en una paleta neutra, un gradiente plata es demasiado tenue para "pegar"; un acento de color **acotado a un solo elemento** sí destaca sin romper la neutralidad general. El default del átomo se dejó en plata (genérico); el color vive en el override del consumidor. |
| Botones sociales duplicados sin atomizar | El login tenía dos `TouchableOpacity flex-row` casi idénticos (Google claro / Apple oscuro) inline — el mismo patrón que `FilterPill`/`IconTile`. | Se atomizó en `features/auth/SocialAuthButton` (`variant: 'light' \| 'dark'`, ícono como `ReactNode`). Antes de repetir un botón con ícono + label, atomizar. |

| Privacidad/Términos: maqueta azul vs. paleta real | La maqueta de `PrivacyTermsStep` (paso de onboarding) venía 100% en azul (escudo con glow, toggle, botón Continuar). Pero la pantalla NO es módulo (no tiene `lime`/`amber`/`rose`); `colors.md` → "resto de la UI" = `zinc` neutro, igual que login y Perfil (sus vecinos en el flujo de auth/onboarding). | Se consultó al usuario y eligió **zinc neutro mono** (mismo criterio que login/Perfil): escudo `shield-outline` blanco sobre círculos `zinc-900`/`zinc-800` (sin glow azul), toggle con track activo `zinc-400`/thumb blanco, CTA sólido `bg-zinc-50` + texto `zinc-950`. El azul no se tradujo a un acento de módulo porque no hay módulo al cual traducir. La vista también migró de `slate` light/dark a `zinc` dark-only (color directo, sin `dark:`). |
| Privacidad: acceso legal y chevron, decisión del usuario | **Primer intento:** se mantuvo el modal de Política de Privacidad (texto legal real Ley 18.331/ARCO/AES-256) y se le dio acceso desde las 3 filas (chevron) + un link en la nota del toggle. **Corrección del usuario:** pidió quitar el chevron/acceso **de las filas**. Yo lo malinterpreté y borré también el link de la nota + el modal entero. **Segunda corrección:** el usuario aclaró que el acceso de la nota del toggle SÍ debía quedar — solo había que sacarlo de los items. | Estado final: las 3 filas son **informativas** (sin `TouchableOpacity`, sin chevron) y el **modal se conserva**, accesible solo desde el link "Política de Privacidad" de la nota del toggle. Lección: cuando el usuario dice "sacalo de los items", acotar el cambio EXACTO a los items; no extrapolar a borrar el componente entero ni otros accesos. Releer el pedido literal antes de ampliar el alcance. |
| Privacidad: items muy altos cortaban el final | El hero (círculos `w-40`/`w-28`, ícono 64) + filas `py-4` + márgenes `mb-10`/`mb-8` empujaban la card de consentimiento y el botón fuera de pantalla en equipos chicos. | Se compactó: hero `w-32`/`w-24` ícono 52, filas `py-3` con `IconTile size=48`, márgenes a `mb-6` y paddings del scroll a 16. La última sección entra sin scroll en pantallas estándar. No agrandar el hero "porque la maqueta lo muestra grande" si come el espacio del CTA. |

| Detalle de sueño: la maqueta duplicaba la duración en minutos ("480 min") | La card "Descanso" de la maqueta mostraba `8 h \| 480 min` (la misma duración expresada dos veces, redundante). El usuario pidió explícitamente **no** agregar el "480 min" y balancear la card para que se vea bien sin eso. | `SleepDetailView` muestra solo `{h} h {min} min` (la duración real, con minutos solo si los hay) centrada en la card, divisor, y fila "Calidad … {label}". No se copió el segundo número redundante. Misma regla "la maqueta no es ley": cuando un dato es redundante o el usuario lo veta, se omite y se rebalancea el layout. |

| Rediseño Home: "rutina realizada hoy" sin día de semana en el modelo | El usuario pidió ocultar la card de rutina si "ya está hecha". Pero `RoutineDay` (rutina activa real) NO tiene día de la semana: `day` es `"Día 1 - Pecho"`, sin mapeo a calendario (el `dayOfWeek` que parece existir es de `AdaptRoutineDay`, otro DTO de adaptación). No hay forma de saber "qué toca entrenar hoy". | Se consultó al usuario y eligió: "realizada hoy" = existe una sesión en el historial (`fetchTrainingHistory`) con `trainedAt` = hoy (único dato real). `useHomeDashboard` trae las 5 sesiones más recientes y chequea `toDateKey(trainedAt) === todayKey` en cliente (más robusto que filtrar por fecha en el backend, por zona horaria). No se inventó un calendario de rutina. |
| Rediseño Home: binarios desaparecen, acumulativos quedan | El pedido "registrar y que se quite" aplica limpio a datos BINARIOS por día (sueño, ánimo: un registro y listo), pero hidratación/nutrición/meditación son ACUMULATIVOS (se van sumando, nunca "terminan"). Tratar todo igual habría hecho desaparecer cosas que el usuario quiere seguir registrando. | Dashboard de dos zonas: **"Para hoy"** (rutina si no entrenó, `MoodQuickCard`, `SleepQuickCard` — se ocultan al completarse vía `wellness.mood`/`wellness.sleep`/`trainedToday`) y **"Tu día"** (`NutritionTodayCard`, `HydrationQuickAddCard` — siempre visibles con progreso). El usuario eligió explícitamente "acumulativos siempre accesibles con progreso". |
| Rediseño Home: registro inline, pero sueño no es de un toque | El usuario eligió registro INLINE (sin navegar). Ánimo (5 niveles) y agua (+250/+500) son de un toque y persisten directo. Pero sueño necesita DOS datos (duración + calidad), y un `WheelPicker` pelea con el scroll del home (lección documentada). | `MoodQuickCard` = 5 emojis de un toque (POST inmediato); `HydrationQuickAddCard` = atajos +250/+500 (bebida=Agua). `SleepQuickCard` = card expandible con `QuantityStepper` de horas + `SelectablePill` de calidad (no wheel). El form completo (nota, minutos finos, otras bebidas) sigue en Salud. Tras cada POST: `bumpWellnessData()` + `wellness.refresh()` para que Salud refresque al volver. |
| Rediseño Home: átomos compartidos sin acento `rose` | Salud = `rose-400` (colors.md), pero `QuantityStepper` y `SelectablePill` solo tenían `lime/amber/mono`. Tentación de parchear el color local dentro de `SleepQuickCard`. | Se extendió el átomo en la fuente (se agregó `rose` a `ACCENT_COLORS`/`SELECTED_STYLES`), no un parche local — misma regla que la migración slate→zinc ("arreglar el átomo, no tintar en el consumidor"). Cambio aditivo no-breaking (`tsc` 0 errores). |
| Rediseño Home: `ModuleCard` huérfano tras reemplazar el dashboard | El home viejo se armaba con `ModuleCard` (3 cards genéricas). El rediseño lo reemplaza por cards específicas por dato. | `grep \bModuleCard\b` confirmó que el único consumidor real era `app/(tabs)/index.tsx` (el `ModuleCard` que aparecía en `ModuleSelectionStep` es un componente LOCAL homónimo, no el del home) → se borró `features/home/ModuleCard.tsx`. Ojo con homónimos: verificar que el match sea el mismo componente, no un local con igual nombre. |
| `[id].tsx` de Salud ya ocupado por el detalle de medición | El detalle de sueño necesitaba ruta dinámica, pero `app/(tabs)/health/[id].tsx` ya es el detalle de `BodyMeasurement` (stack flat de `(tabs)/health`). Reusar `[id]` habría colisionado. | Se creó una ruta nombrada `sleep-detail.tsx` (registrada en `_layout.tsx`) y la navegación pasa `{ id, data: JSON.stringify(log) }` como params — mismo patrón de snapshot+refetch que el `[id].tsx` de medición (render inmediato con el snapshot del param, refresca por `id` con `getSleepLogById`). Antes de usar `[id]` en un stack flat, verificar que no esté tomado por otra entidad. |
| Detalle de sueño: delete + refresh sin hook nuevo | El detalle necesitaba eliminar (`DELETE /api/sleep/{id}`) y que la lista se actualice al volver. `useSleepHistory` no expone delete y crear un hook solo para esto era overkill. | El delete se hace inline en `sleep-detail.tsx` (`Alert.alert` de confirmación destructiva → `deleteSleepLog` → `router.back()`), reusando el service existente. La lista se refresca sola porque `sleep.tsx` ya tiene `useFocusEffect(refresh)`. No se agregó estado de borrado al hook de historial: el detalle es dueño de su propia acción. |
| `useFocusEffect(refresh)` incondicional refetchea al pedo al volver de una vista de lectura | `wellness.tsx` y `sleep.tsx` refrescaban en CADA focus posterior al primero (con un `didInitialFocusRef`). Volver de una vista de solo lectura (ej. Bienestar → vista de Sueño → volver, o Sueño → detalle → volver) disparaba una llamada a la API sin que nada hubiera cambiado. | Se creó `src/store/wellness-sync.ts` (módulo con `version` + `bumpWellnessData()`/`getWellnessDataVersion()`, mismo estilo que `training-history-cache`). Las MUTACIONES la incrementan: `useSleepHistory.submit` (post) y el delete de `sleep-detail`. Cada pantalla guarda `lastVersionRef = useRef(getWellnessDataVersion())` y en `useFocusEffect` solo llama `refresh()` si la versión cambió. Una sola versión global sirve a varias pantallas porque cada una tiene su propio `lastVersionRef` (a diferencia de un boolean "dirty" que la primera pantalla consumiría). Al agregar forms de hidratación/ánimo/meditación, sus mutaciones también deben llamar `bumpWellnessData()`. Regla general: refrescar en focus SOLO ante mutaciones reales, no en cada navegación. |

| Datos básicos (paso 1): maqueta azul + indicador numerado, paleta real zinc + indicador original | La maqueta de "Datos básicos" venía 100% en azul (género seleccionado, botón Continuar) y con un **indicador de pasos numerado** (círculos 1-2-…-6 con check). Onboarding NO es módulo (`colors.md` → "resto de la UI" = `zinc` neutro, igual que login/Perfil/PrivacyTermsStep, sus vecinos en el flujo). Además el usuario pidió **mantener el indicador original** (el `ProgressBar` de segmentos), no el numerado de la img. | El azul → `zinc` neutro mono (CTA `OnboardingFooter` ya es `bg-zinc-50`/`text-zinc-950` en dark; género con `SegmentedControl accent="mono"`). Se **conservó `ProgressBar`** (no se dibujó el indicador numerado). La vista migró de `slate` light/dark a dark-only `zinc` (color directo). Mismo criterio de siempre: no copiar el azul literal ni inventar un control que el usuario excluyó. |
| Género con solo 2 opciones y sin preselección, reusando `SegmentedControl` | La maqueta mostraba 3 segmentos (Masculino/Femenino/Otro) con uno preseleccionado. El usuario aclaró: **solo 2 géneros (sin "Otro")** y que **no arranque con uno seleccionado**. Tentación de inventar un segmento "No seleccionado" o un control nuevo. | Se reusó el átomo `SegmentedControl` con SOLO `[Masculino, Femenino]`: como resalta el segmento cuyo `value` matchea, con `gender=''` inicial **ningún segmento queda resaltado** = estado "no seleccionado" gratis, sin tercer segmento fantasma. Antes de inventar un control, ver si el átomo existente ya cubre el caso por composición. |
| `alert` de validación inalcanzable tras `disabled` | `BasicInfoStep1` tenía `handleContinue` con `if (!gender) alert(...)`, pero el botón ya era `disabled={!gender}` en `OnboardingFooter` → el `alert` nunca se alcanza (código muerto). Un test viejo lo afirmaba y fallaba (TouchableOpacity deshabilitado no dispara `onPress`). | El guard real es el **botón deshabilitado** (mejor UX que un `alert`). Se mantuvo `disabled` y el test se ajustó a verificar "deshabilitado → no avanza", no el alert. Cuando un botón ya está `disabled` por la misma condición, no duplicar el guard con un `alert` inalcanzable. |

| Datos físicos (paso 2): wheel vertical de la maqueta vs. conflicto de gesto | La maqueta de "Datos físicos" mostraba Peso/Altura como **wheel vertical** (números apilados), pero la vista actual usaba `RulerPicker` (regla **horizontal**). Un wheel vertical hecho en JS (ScrollView vertical con snap) viviría dentro del `ScrollView` vertical de la página → **conflicto de gesto** (misma dirección), justo lo que advierte la "Regla para controles interactivos anidados". El `RulerPicker` lo esquivaba por ser horizontal (gestos ortogonales). | Se **consultó al usuario** y pidió **control nativo en iOS y Android**. Se atomizó `common/WheelPicker` sobre `@react-native-picker/picker` (ya dependencia): **iOS = wheel girable nativo** (idéntico a la maqueta), **Android = dropdown/diálogo nativo** (Android stock NO tiene wheel girable; degradación honesta). Al ser nativo, no pelea con el responder system del scroll padre. Se aclaró al usuario que en Android no se ve la rueda. No se metió un wheel JS para no caer en el conflicto de gesto documentado. |
| Paso 2 redibujado sin botón "Atrás" (fiel a la maqueta) | El `BasicInfoStep2` viejo dibujaba un botón "Atrás" (`chevron-back` + texto), pero la maqueta de "Datos físicos" no muestra ningún botón de retroceso. | Se quitó el botón visible: el back ya está cubierto por `SwipeBackWrapper` (lo envuelve `onboarding.tsx`) y por consistencia con el paso 1 (que tampoco tiene back). El prop `onBack` se mantiene en la interfaz (lo pasa el contenedor) pero ya no se renderiza. Fidelidad a la maqueta sin romper la navegación. |

| Wheel nativo come el gesto vertical → las secciones deben ENTRAR en pantalla | Con dos `WheelPicker` (Peso/Altura) y el alto original (180px c/u), la card de Altura quedaba cortada abajo. No alcanza con "que scrollee": el wheel **nativo captura el pan vertical**, así que el usuario no puede arrastrar sobre el wheel para scrollear la página hasta Altura (el wheel gana el gesto en su área). | El alto del wheel se hizo **prop `wheelHeight`** y `BasicInfoStep2` lo calcula **responsive** (`(screenHeight - 580) / 2`, acotado 96–160) para que las dos cards entren sin scroll en teléfonos modernos; además se compactó (header `pt-3`, cards `p-4`, valor `text-4xl`). Cuando un control nativo scrolleable ocupa la pantalla, el layout tiene que **caber**, no depender del scroll del padre. |

| Objetivos como "items" de la maqueta, pero `Goal` no tiene descripción | El usuario pidió presentar los objetivos del paso 3 con el **estilo de lista de items** de la maqueta (card con título uppercase + descripción + selección por borde). Pero `Goal` solo trae `{ id, name }` — **no hay descripción**. Copiar la descripción de la maqueta sería inventar (mismo caso que "Marca/Nivel inventados"). | Se aplicó el **estilo** (card `zinc-900`, título uppercase a la izquierda, borde `zinc-100` al seleccionar) pero **solo con el título**; la descripción se omite hasta que el backend la exponga. La maqueta original era de "Experiencia" (Fitness), usada solo como **referencia de estilo**, no de contenido. |
| Estilo de lista nuevo: extender `SelectableCard`, no forkear | La lista de items de la maqueta (outline, izquierda) difería del `SelectableCard` existente (relleno, centrado). Tentación de crear un componente paralelo (error tipo `FilterPill`). | Se **extendió `SelectableCard` con `variant: 'filled' | 'outline'`** (default `filled` → su único consumidor, `FitnessConfigStep`, queda intacto) en vez de crear uno nuevo. El paso 3 usa `variant="outline"`. Antes de crear un card seleccionable paralelo, ver si el átomo existente se puede extender con una variante sin romper a sus consumidores. |
| Uppercase del título: CSS, no transformar el string | El estilo de la maqueta muestra el título en mayúsculas, pero el test del paso 3 hace `getByText('Bajar de peso')` (texto original). Transformar con `.toUpperCase()` rompería el match. | Se usó la clase `uppercase` (CSS, solo visual): el nodo de texto sigue siendo "Bajar de peso" y el test pasa. Para mayúsculas de estilo, usar `uppercase` de CSS, no transformar el string que los tests buscan. |
| Guard del paso 3: alert (no disabled) por contrato de test | Los pasos 1/2 usan `disabled={!valor}` en Continuar (el alert sería código muerto). Pero el test del paso 3 ya esperaba el **alert** "Por favor selecciona tu objetivo." al presionar sin objetivo → exige el botón habilitado. | El paso 3 mantiene el botón **habilitado** (`disabled={isSubmitting}`) con guard por `alert`, respetando el contrato del test existente. Verificar el test antes de decidir disabled vs alert: el contrato puede diferir entre pasos. |

| Selección de módulos: alinear el chrome pero NO tocar las cards (excepción a "azul → acento") | `ModuleSelectionStep` ("Elige tu enfoque") seguía en `slate` con header a la izquierda, mientras los pasos 1/2/3 ya eran `zinc` neutro con header centrado. La regla habitual sería traducir todo el azul/degradado de las cards al acento neutro. | El usuario pidió explícitamente **dejar las tarjetas de módulo (imagen + degradado azul `#0F172B`) tal cual** y solo alinear el resto: `bg-zinc-950`, header `centered`, `pt-6`, loading `zinc`, helper del footer = círculo `zinc-900`/`information-circle-outline` (igual que `BasicInfoStep3`). Se mantuvo el `ProgressBar` original (no el indicador numerado de la img). La regla "maqueta azul → acento" admite excepción cuando el usuario fija que un bloque visual se conserva; acotar el cambio al chrome y no extrapolar a las cards. De paso se borró el `cssInterop(Ionicons)` que quedó huérfano al pasar el helper icon de `className` a `color`. |

| Onboarding Salud: fusionar buscador + chips en una card y atomizar el patrón | La maqueta de "Datos de salud" mostraba **una sola card** (título + buscador con lupa + ítems con X circular), pero `SearchableSelect` (el átomo de búsqueda+selección) renderizaba **dos cards separadas** (buscador con chevron + card de chips "Seleccionadas (N)/Borrar todas") en `slate` light/dark con un link azul `#3b82f6`. Ese átomo se usa en **2 vistas**: onboarding Salud (`HealthConfigStep`) y Perfil (`InjuriesConfig`/`ConditionsConfig`). | Se reescribió `SearchableSelect` en `zinc` dark-only fusionando todo en una card `bg-zinc-900 rounded-3xl`: buscador con **lupa a la izquierda** (sin chevron), ítems seleccionados como filas con divisor y **botón circular** `border-zinc-600` para quitar. Como es el átomo compartido, **ambas vistas se rediseñan a la vez** (DRY: no se forkeó uno paralelo). El acento de módulo (rose) NO se bakeó en el átomo — queda **neutro zinc** y el consumidor pone el color (footer/spinner). Se quitó el prop `selectedLabel` (ya no hay header de seleccionados) → se borró de los 2 consumidores de Perfil. El onboarding NO pasa `cardIconName`/`cardSubtitle` (look maqueta); Perfil sí (backwards-compat). |
| Onboarding Salud: conservar el dot de severidad que la maqueta esconde | La maqueta de "Datos de salud" lista los ítems **solo con el nombre** (sin color), pero los `HealthItem` traen `severity` (`Low`/`Medium`/`High`) con un dot de color real (teal/orange/red). El usuario pidió **conservar/agregar ese color** aunque la maqueta no lo muestre. | Se mantuvo el dot de severidad por ítem (tanto en el dropdown como en los seleccionados) y se **quitó el sufijo "- Severity"** del texto para igualar el look limpio de la maqueta. El dot es **dato de gravedad, NO acento de módulo** — por eso sigue teal/orange/red y no rose. Caso inverso a "no inventar lo que la maqueta pide de más": acá la maqueta omite un dato real que SÍ se conserva por pedido explícito. |
| Onboarding Salud: CTA mono zinc, no rose (consistencia del flujo) | La maqueta del "Continuar" venía azul/gradiente. Tentación de pintarlo con el acento del módulo (rose-400, como los CTA de Salud in-app). Pero **dentro del onboarding** todos los pasos comparten el `OnboardingFooter` mono (`bg-zinc-50`/`text-zinc-950`, `colors.md` → acción = acento global). | Se **consultó al usuario** y eligió **mono zinc** (consistencia con el resto del onboarding + `colors.md`). El acento rose del módulo se inyecta solo en detalles: el candado del helper (`lock-closed-outline` rose-400 `#fb7185` sobre `zinc-900`) y el spinner de carga. El header se pasó a `centered` (maqueta) y se conservó el `ProgressBar` original (no el indicador numerado de la img). No copiar el azul ni asumir que un módulo dentro del onboarding pinta su CTA con su acento. |
| Status bar invisible en app dark-only (`style="auto"`/`"dark"`) | La status bar no se veía en varias vistas: el root (`app/_layout.tsx`) usaba `<StatusBar style="auto" />` y `HealthConfigStep` la pisaba con `style="dark"` (íconos/texto **oscuros** sobre fondo oscuro = invisibles). El último `StatusBar` montado gana, así que el override `dark` rompía el onboarding aunque el root fuera `auto`. | Como la app es **dark-only** (`userInterfaceStyle: dark` forzado), el contenido de la status bar siempre debe ser **claro**: root → `<StatusBar style="light" />` (cubre todas las vistas globalmente) y se corrigieron los `style="dark"` de `HealthConfigStep` a `light`. Regla: en app dark-only no usar `style="auto"` ni `"dark"` — fijar `"light"` para garantizar contraste blanco visible. |

| Limpieza transversal `slate`→`zinc` del chrome de onboarding | Tras rediseñar la vista de Salud, el resto del onboarding seguía en `slate` light/dark vía átomos **compartidos** (`OnboardingHeader`/`OnboardingFooter`/`ProgressBar`/`SectionCard`/`TagSelect`) + el contenedor `onboarding.tsx` y los step files de Nutrición/Fitness. El usuario pidió "que quede bien limpio" → migración completa, no parcial. | Se migraron los 5 átomos compartidos a `zinc` dark-only (color directo, sin `dark:` ni variantes light) + el contenedor + `ModuleConfigRouter` + los step files (iconos `text-slate-500`→`text-zinc-400`, loaders, listas inline). Como son átomos compartidos, **todo el flujo (6 pasos) + los configs de Perfil se limpian de una**. `useColorScheme`/`isDark` quedaron huérfanos en `TagSelect` (placeholder ya no ramifica por tema) → borrados. Mapeo: `bg-white dark:bg-slate-900`→`bg-zinc-900`, `border-slate-200 dark:border-slate-800`→`border-zinc-800`, `text-slate-900 dark:text-slate-50`→`text-white`, `text-slate-500 dark:text-slate-400`→`text-zinc-400`. Se conservó el prop `inverted` de los átomos (backwards-compat) aunque en dark-only ambas ramas converjan. |
| Lista de seleccionados duplicada idéntica en 2 steps → atomizar | El bloque "Seleccionadas (N)/Borrar todas + filas con X" estaba **copiado idéntico** en `NutritionAllergyStep` y `NutritionDietStyleStep` (ambos con `TagSelect showSelectedList={false}` + lista manual) — el mismo error que `FilterPill`/`IconTile`. | Se atomizó en `common/SelectedItemList` (filas con nombre + botón circular para quitar, mismo lenguaje visual que `SearchableSelect`), consumido por ambos steps. **No** se forzó al caso de Fitness "equipamiento" porque ese trae un stepper `−/+` de cantidad por ítem (estructura distinta) → ese bloque quedó inline, solo migrado de color. Antes de copiar un bloque de "lista seleccionada", ver si ya hay (o conviene crear) un átomo; y no meter a la fuerza en el átomo un caso que diverge (el de cantidad). |
| `TagSelect` y `SearchableSelect`: misma mecánica, NO unificar todavía | Ambos son buscador + dropdown + selección, casi gemelos. Tentación de fusionarlos en uno. | Se dejaron separados: difieren en el **tipo de ítem** (`TagSelect` `{id,name}` vs `SearchableSelect` `HealthItem` con `severity`) y en el **render de la selección** (chips/`SelectedItemList` vs card fusionada con dot de severidad). Unificar ahora agregaría props de un solo uso (el costo > el beneficio con 2 consumidores). Se documentó la deuda: si aparece un 3.º con las mismas necesidades, recién ahí extraer un buscador base común (mismo criterio que `BottomSheetModal` vs los sheets con handle). |
| Tests viejos en rojo en la branch de rediseño (no introducidos por el cambio) | Al correr los tests de onboarding, 3 suites (`ModuleSelectionStep`, `NutritionConfigStep`, `FitnessConfigStep`) fallaban (7 tests). Riesgo de asumir que la limpieza `slate`→`zinc` los rompió. | Se verificó con `git stash -u` + correr contra el último commit: **las mismas 3 suites / 7 tests ya fallaban antes** de tocar nada (assertions desactualizadas del rediseño en curso: buscan `'Tu objetivo'` en flujos donde no va, `'¿Tienes alguna alergia?'` con `¿` cuando el título es sin `¿`, `'Experiencia completa'` que ya no existe). La limpieza introdujo **0 fallas nuevas**. Lección: antes de "arreglar" un test rojo tras un cambio de estilo, confirmar el baseline (stash + run) — el rojo puede ser preexistente de WIP de la branch, no del cambio actual. No se tocaron esos tests (fuera de scope; son del rediseño de esos steps, aún no hecho). |

| Onboarding Fitness (disponibilidad/duración): de cards a campos "al aire" + átomos compartidos | El subStep 2 (`FitnessConfigStep`) usaba `SectionCard` (cards con ícono) + `WeekDayPicker` cuadrado `slate` + `RulerPicker` horizontal, mientras la maqueta pide el estilo "eyebrow + pregunta" (como subStep 0/1), días en **círculos** con acento y la duración como **rueda**. El `WeekDayPicker` lo comparten onboarding **y** perfil (`FitnessTrainingPreferencesConfig`). | Se migró el subStep 2 a `FieldSection` (header `centered`, divisor `h-px`, padding 24/24 como subStep 0/1), el día a `WeekDayPicker` rediseñado (círculo `rounded-full`, dark-only `zinc`, prop `accent`; se quitó el rojo de fin de semana que la maqueta no usa) y la duración a `WheelPicker` (prop `accent` nuevo para el valor grande lime) en la rama "Elegir tiempo". El requisito "Tengo tiempo / Elegir tiempo" se mantuvo con los dos `SelectableCard` dentro del `FieldSection` de duración. Como `WeekDayPicker` es compartido, **se migró el átomo y se actualizó el perfil a la vez** (`accent="lime"`, DRY, no se forkeó). El azul de la maqueta → `lime-400` (regla de siempre) y se conservó el `ProgressBar` original (no el indicador de la img), por pedido del usuario. `RulerPicker` quedó sin uso en `FitnessConfigStep` → se sacó el import. |
| WheelPicker captura el pan vertical también acá (duración) | Igual que en `BasicInfoStep2`, el `WheelPicker` nativo come el gesto vertical en su área. En el subStep 2 solo aparece al elegir "Elegir tiempo" y queda al final del scroll, así que el usuario scrollea con la zona superior. Si en el futuro se agregan más controles debajo, recordar que el wheel no deja arrastrar la página sobre él (ver lección de `BasicInfoStep2`). | Se dejó el `wheelHeight` default (180) porque es un solo wheel y hay margen; no hizo falta el cálculo responsive del paso 2. |

| Onboarding Fitness (equipamiento): fila de cantidad duplicada en 2 vistas + lista interna muerta | El subStep 3 (`FitnessConfigStep`) y el `EquipmentConfig` del perfil renderizaban **cada uno** su propia fila "nombre + stepper −/+ + papelera" (mismo error que `FilterPill`/`IconTile`), y `EquipmentSelect` traía una **tercera** lista de seleccionados interna (`showSelectedList`) que **ningún** consumidor usaba (ambos pasaban `false`) → código muerto. Además `EquipmentSelect` seguía en `slate` light/dark. | Se atomizó `common/EquipmentSelectedList` (encabezado `(N)` + "Borrar todas" + filas con `QuantityStepper` reusado, papelera para quitar) y lo consumen onboarding y perfil (`accent="lime"`, DRY). `EquipmentSelect` se migró a `zinc` dark-only con **lupa a la izquierda** (estilo del buscador nuevo) y se **borró la lista interna muerta + el prop `showSelectedList`** + sus handlers `handleIncrement/Decrement` (grep confirmó que ambos consumidores pasaban `false`). El subStep 3 pasó a `FieldSection`/header centrado/padding 24/24 como el resto del onboarding. `SectionCard`/`TouchableOpacity` quedaron sin uso en `FitnessConfigStep` → imports borrados. |
| Stepper de cantidad: `−` deja de remover, la papelera remueve | El código viejo (onboarding y perfil) **removía el ítem al bajar de 1** con el botón `−`. Al reusar `QuantityStepper` (que se **deshabilita** en `min`), ese comportamiento se pierde. | Se fijó `min={1}` en `EquipmentSelectedList`: el `−` se deshabilita en 1 y la **papelera** es el único modo de quitar (más explícito y consistente con `ConsumedFoodCard`). Cambio de UX deliberado, no un bug. También se mejoró `QuantityStepper` para no imprimir espacio sobrante cuando `unit=""` (equipamiento no tiene unidad). |

| Equipamiento: scroll de página vs scroll de lista (header/buscador deben quedar fijos) | El subStep 3 metía header + buscador + lista en **un único `ScrollView`** envuelto en `<Pressable flex:1>`. Problemas: (1) scrolleaba toda la página, no solo la lista; (2) el `ScrollView` sin `flex:1` propio dentro del `Pressable` se dimensionaba al contenido y **recortaba** los últimos ítems; (3) arrastrar **sobre un ítem** no scrolleaba bien. El usuario pidió: header + buscador fijos y scroll **solo en la lista**. | Se separó en **dos zonas** (mismo patrón que `EquipmentConfig` del perfil): una `<View>` fija arriba (header + `FieldSection` con el buscador, `zIndex:20` para que el dropdown absoluto se superponga a la lista) y un `ScrollView` **dedicado** (`flex:1`) solo para `EquipmentSelectedList`. El `closeDropdowns` pasó de envolver el scroll a un **backdrop absoluto** (`zIndex:0`) + `onScrollBeginDrag`. Cuando no hay ítems, un `<View flex-1/>` empuja el footer abajo. Regla: si querés header fijo + lista scrolleable, NO uses un solo `ScrollView` de página; separá zona fija de zona scrolleable (`ScrollView flex:1` propio) — así además el drag sobre los ítems sí scrollea. Aparte, el `QuantityStepper` tenía los `−/+` pegados al número (`justify-between` no separa cuando el contenedor se dimensiona al contenido) → `gap-3` + `min-w` al valor (no afecta a `ConsumedFoodCard`, donde el stepper va a ancho completo). |

| Onboarding Nutrición (alergias): reusar el átomo de Salud, no clonar el look | El registro de alergias (`NutritionAllergyStep`) armaba la vista con `SectionCard` + `TagSelect` + `SelectedItemList` (dos cards), pero es **la misma vista** que el onboarding de Salud ("buscar + seleccionar de un catálogo"), que ya se resolvió con el átomo `SearchableSelect` (card fusionada: título + buscador con lupa + seleccionados con X circular). Copiar el look de Salud con piezas distintas sería el error `FilterPill`/`SelectablePill`. | Se reusó `SearchableSelect` en `NutritionAllergyStep` espejando `HealthConfigStep` (header `centered` "Datos de nutrición", `cardTitle="¿Tienes alguna alergia?"`, footer con candado en tile `amber-400`). Obstáculo: `SearchableSelect` tipaba `items: HealthItem[]` (exige `severity`) y las alergias son `NutritionItem` (sin severidad). En vez de castear o forkear, se **aflojó el tipo** a un `SelectableItem` (`{ id, name, severity? }`) exportado por el átomo: ya degradaba el dot a gris neutro sin severidad, así que ahora es genuinamente neutro de dominio (`HealthItem` y `NutritionItem` son ambos asignables). `TagSelect`/`SectionCard`/`SelectedItemList` siguen vivos en `NutritionDietStyleStep` (gemelo aún sin migrar) → no quedaron huérfanos. |

| Onboarding Nutrición (estilo de dieta): mismo rediseño que alergias + huérfano `SelectedItemList` | `NutritionDietStyleStep` (subStep 3, gemelo idéntico de alergias) seguía con `SectionCard`+`TagSelect`+`SelectedItemList`. Tras migrar alergias a `SearchableSelect`, dejar el gemelo en el patrón viejo dejaba dos pasos seguidos con diseños distintos. | Se migró igual que `NutritionAllergyStep` (reusa `SearchableSelect`, header centrado "Datos de nutrición", `cardTitle="¿Sigues algún estilo de dieta?"`, footer candado tile `amber-400`). Como `SearchableSelect` ya trae su propia lista de seleccionados embebida, el átomo `SelectedItemList` (lista SIN cantidad) quedó **huérfano** — `grep` confirmó que su único importador era este step (la mención en `EquipmentSelectedList` era solo un comentario) → se **borró**. `TagSelect`/`SectionCard` NO se tocaron: siguen en uso por los configs de Perfil (`DietaryConfig`, `FitnessSubGoalConfig`, `FitnessTrainingPreferencesConfig`). Mismo criterio que `RoutineFilters`/`MacroRing`: grep antes de borrar, y borrar solo lo realmente huérfano. |

| Versionado: el historial dropeaba `routineVersionId`/`routineVersionNumber` aunque el backend los manda | El doc de versionado dice que `GET /api/routine/training-history` ahora devuelve `routineVersionId` + `routineVersionNumber` por sesión, pero el `TrainingHistorySessionDto` NO los declaraba y `mapSessionDto` no los copiaba → el frontend los **descartaba en silencio** y la versión nunca aparecía en el detalle ni en las cards del historial. Lo reportó el usuario ("la sesión no me muestra la versión"). | Se agregaron al DTO (`routineVersionNumber?: number \| string \| null`, defensivo porque el resto del DTO usa strings) y al dominio (`routineVersionNumber: number \| null`), el mapper coacciona con `Number(x) \| null` (versiones arrancan en 1 → descarta 0/NaN sin perder válidos) y la UI muestra "Versión N" en el detalle y `vN` en `SwipeableTrainingHistoryCard`/`RecentActivityCard`. Lección: cuando un contrato nuevo agrega campos a un endpoint **ya mapeado**, revisar el DTO+mapper de ese endpoint, no solo las pantallas nuevas — un mapper viejo dropea campos sin error. |
| Versionado: reusar la vista animada para el preview sin romper la máquina de swipe | El pedido fue mostrar una versión histórica en la **misma** `RoutineDetailView` (FlatList horizontal paginado por día + cross-fade de `DaySlot` con shared values `scrollX`/`baseOffset`). Tentación 1: forkear una vista read-only (duplica el render de días, error tipo `FilterPill`). Tentación 2: swapear el `data` del FlatList sin tocar el índice → al venir de una versión con otra cantidad de días, el offset viejo (p. ej. día 2) quedaba fuera de rango y el header de días desincronizaba. | Una sola fuente de verdad: `sortedDays = useMemo(previewVersion ? previewVersion.days : routine.days)`. Al **transicionar** preview (con un `prevPreviewRef` que evita pisar el "hoy" inicial en el mount) se resetea `activeDayIndex=0` + `scrollToIndex(0, animated:false)` dentro de `requestAnimationFrame` y envuelto en `try/catch` (la lista puede no haber medido). El `isActive` para decidir el CTA sale del **propio `RoutineVersionDetail`** (no de `routine.activeVersionId`, que puede faltar si el backend viejo no lo manda). Los CTAs de versión (`Activar`/`Restaurar`) se renderizan **aunque la rutina sea `readOnly`** (condición `isPreviewing`, independiente de `readOnly`), porque activar/restaurar una versión es válido en cualquier rutina. Activar arma el `Routine` actualizado localmente desde el detalle devuelto (no re-fetch): `activeVersionId=detail.id`; restaurar además fija `latestVersionId=detail.id` (crea la nueva última). |

| Archivo de ruta creado FUERA del repo por dropear `Frontend` del path | El proyecto Expo vive en `D:\...\app\Frontend`, pero existe también el contenedor `D:\...\app\`. Al escribir `app/(tabs)/health/clinical-readings.tsx` con el path absoluto **sin** el segmento `Frontend\app`, el archivo se creó en `D:\...\app\(tabs)\health\...` (fuera del proyecto) → Expo Router nunca lo habría visto. | Usar SIEMPRE el path absoluto completo (`...\app\Frontend\app\(tabs)\...`). Tras crear archivos de ruta nuevos, verificar con `find app -name "*.tsx" | grep -v Frontend` que no quedó nada fuera de `Frontend`; si quedó, moverlo y limpiar los dirs vacíos. |

| Bienestar: enums string, NO numéricos como el resto de la API | El módulo Clínico (y el resto del proyecto) serializa enums como **números** (`bloodType` 0-3, `rhFactor` 0-1). El contrato de Wellness dice explícitamente lo contrario: `quality`/`mood`/`beverageType`/`technique` van como **strings exactos** (`"Good"`, `"Water"`, `"Mindfulness"`). Asumir números (como en clínico) habría roto los POST y el match de labels. | `types/wellness.ts` define los enums como **uniones de literales string** (`type SleepQuality = "VeryPoor" | ... | "Excellent"`) y los labels ES con `Record<Enum,string>`. No copiar el patrón numérico del clínico: verificar el contrato de CADA módulo (mismo criterio "verificá el DTO real"). |
| Bienestar: "Hoy" sin endpoint de stats → derivar de la página 1 | La maqueta muestra un resumen "Hoy" (sueño, ml de hidratación, ánimo, minutos de meditación), pero el contrato (FAQ) aclara que **no hay endpoint de agregación/estadísticas** — los datos son brutos. Inventar un GET de resumen o un total del backend sería suponer algo que no existe. | `useWellnessDashboard` pide página 1 de las 4 features y **deriva "Hoy" en cliente** filtrando por `getTodayDateKey()` (sueño/ánimo = primer registro de hoy; hidratación/meditación = **suma** de los de hoy). Es agregación de presentación sobre datos ya devueltos (NO cálculo de salud de `agent.md §7`), mismo criterio que `computeSessionStats`. La "Actividad reciente" es el merge de las 4 ordenado por `capturedAt` desc. |
| Bienestar: hidratación se llama distinto según la sección (fiel a la maqueta) | La maqueta titula la fila de hidratación como **"Hidratación"** en "Hoy" y en "Registrar", pero como **"Agua"** en "Actividad reciente". Unificar a un solo label habría sido más "limpio" pero infiel a la maqueta. | Se respetó la divergencia: las filas de "Hoy"/"Registrar" usan "Hidratación" (hardcode en `WellnessView`) y `getActivityTitle('hydration')` devuelve "Agua" para la actividad reciente. Fidelidad a la maqueta por encima de la consistencia de naming. |
| Bienestar: contrato completo en datos, solo 1 vista en UI (por pedido) | El contrato cubre 4 features × (POST/GET list/GET id/DELETE) + catálogo de guías, y la maqueta insinúa formularios de registro, detalles por registro y catálogo de guías. El usuario pidió **implementar todo el contrato pero construir solo la vista de la maqueta** (Bienestar), no las demás. | `wellness.service.ts` implementa **todos** los endpoints (aunque la UI de ahora solo use los GET de listado) y `types/wellness.ts` todos los DTOs/payloads; pero solo se creó `WellnessView` + sus átomos. Los callbacks de registro/detalle de la vista son **opcionales** y hoy quedan sin cablear (TODO): las filas/cards conservan el chevron pero no navegan hasta que existan esas pantallas. No se crearon hooks por-feature con `submit/delete/loadMore` para no dejar hooks huérfanos; van con sus formularios. |
| Sueño (detalle): reusar el patrón del hermano clínico, no inventar | El detalle de Sueño (`/health/sleep`: header + "Registrar" + "Último registro" + "Historial" paginado) es estructuralmente **idéntico** a `clinical/ClinicalReadingsView` (header back circular + card registrar + lista paginada). Tentación de armar todo desde cero. | Se espejó el patrón clínico: `SleepView` clona el layout de `ClinicalReadingsView` (back circular `w-11`, `bottomOffset = insets.bottom + TAB_BAR_HEIGHT + 16`), `SleepRegisterCard` clona `RegisterReadingCard`, y `useSleepHistory` clona `useClinicalReadings` (getTokenRef, carga en mount, `loadMore`, **sin** `submit` porque el form es fase futura). El "Historial" reusa `WellnessGroupCard` + `WellnessLogRow` (no se creó una fila paralela). Antes de construir una pantalla nueva, buscar el hermano ya resuelto en otro submódulo y espejarlo. |
| Sueño: "Nuevo registro" placeholder + filas sin chevron (honestidad sobre la maqueta) | La maqueta muestra el botón "Nuevo registro" activo y un chevron en cada fila del historial, pero el usuario eligió "solo Sueño, **sin form todavía**" y NO hay pantalla de detalle por registro. Pintar un botón/chevron que no llevan a ningún lado engaña. | `SleepRegisterCard` con `onPress?` opcional: sin handler queda **deshabilitado** (`opacity-60`), mismo criterio que "Scanner placeholder/deshabilitada". Las filas del historial pasan `showChevron={false}` (no hay detalle), mismo criterio que `ClinicalReadingHistoryCard` ("sin tap/chevron: el detalle no está implementado"). Para soportar el "sin chevron" sin clonar la fila, se **extendió** `WellnessLogRow` con `showChevron?` (default true, no rompe a "Hoy"/"Actividad reciente") + `note?` + `subtitle: ReactNode` (resaltar la calidad en rose). El azul de la maqueta → `rose-400` (regla de siempre). |
| Sueño: navegación reusa la card "Registrar Sueño" y la fila "Hoy" del dashboard | El detalle de Sueño necesitaba un punto de entrada. La maqueta del dashboard ya tiene la fila "Sueño" en "Hoy" y la card "Sueño" en "Registrar", ambas sin cablear. | Se reemplazó el prop `onRegisterSleep` (que apuntaba a un form inexistente) por `onOpenSleep` y se cableó en **ambos** lugares (fila Hoy + card Registrar) → `router.push('/health/sleep')`. Las otras 3 features mantienen `onRegisterX?` sin cablear (fase futura). No se inventó una card de entrada nueva: el hub de Sueño es el destino común de "ver" y "registrar". |

| Form "Registrar sueño": espejar el molde clínico, no construir de cero | El form de la maqueta (Fecha + Duración + Calidad + Nota + CTA) es estructuralmente **idéntico** a `ClinicalReadingFormView` (mismo módulo Salud, mismo commit reciente): header back circular, card Fecha con `DateTimePicker` (spinner iOS / default Android, `maximumDate=hoy`), info banner, CTA sólido `rose-400` dentro del scroll con `paddingBottom = insets.bottom + TAB_BAR_HEIGHT + 8` (vive en el Stack de `(tabs)/health`). | Se espejó: `SleepFormView` clona el layout de `ClinicalReadingFormView`, la pantalla `sleep-new.tsx` clona `clinical-reading-new.tsx` y el hook se **extendió** (`useSleepHistory` ganó `autoLoad`/`isSubmitting`/`submitError`/`submit`, igual que `useClinicalReadings` sirve historial **y** registro en un solo hook). No se creó un hook `useCreateSleepLog` paralelo. La calidad reusa `SegmentedControl accent="rose" variant="solid"` (5 opciones de `SLEEP_QUALITY_LABELS`), no un selector de pills nuevo. El azul de la maqueta → `rose-400` (regla de siempre). `toDateKey` se subió a `wellness.utils` (estaba inline en el form clínico) para reusarlo. |
| Sueño: picker de duración nativo (h+min), NO wheel JS | La maqueta dibuja la Duración como dos wheels verticales (horas/minutos) con banda resaltada — visualmente un wheel JS. Pero un wheel JS (ScrollView vertical con snap) dentro del `ScrollView` de la página entra en **conflicto de gesto** (misma dirección), justo lo que ya documentaron `BasicInfoStep2`/`FitnessConfigStep`. `WheelPicker` (átomo existente) es de **un solo valor** (valor grande arriba + unidad a la derecha): no cubre dos columnas h+min. | Se creó el átomo `common/DurationWheelPicker` sobre `@react-native-picker/picker` (nativo → no pelea con el responder system): **iOS** dos wheels girables con banda + divisor; **Android** dos dropdowns. Genérico (`accent`/`maxHours`/`minuteStep`) para reusarse a futuro en Meditación. **No** es un fork de `WheelPicker` (control distinto: dual-column duración vs. valor único). El total ("480 min") lo computa y muestra el form, no el átomo. **Limitación honesta:** `Picker.Item` colorea TODOS los ítems igual (no solo el centrado), así que los off-center quedan en acento atenuado por el fade nativo, no grises como la maqueta — costo aceptado por no caer en el wheel JS prohibido. |
| Sueño: nota máx 120 (maqueta) vs 500 (contrato) | La maqueta muestra el contador `34/120` (máx 120), pero el contrato dice `note` máx 500. Conflicto maqueta ↔ contrato. | El usuario pidió "solo la funcionalidad de la img" → se respetó el límite de la **maqueta** (120) con `maxLength` en el `TextInput`. 120 entra holgado dentro del tope del backend (500), así que no hay riesgo de 400. Cuando la maqueta es **más restrictiva** que el contrato y ambos son válidos, seguir la maqueta (mismo criterio "fidelidad a la maqueta"); documentar el porqué para que no se "corrija" a 500 luego. |
| Dashboard de Salud: `useFocusEffect` con `didInitialFocusRef` refetcheaba al volver de una vista de lectura | Mismo síntoma que #153 (Bienestar), pero en el **dashboard de Salud** (`app/(tabs)/health/index.tsx`): el `useFocusEffect` saltaba el primer focus con un `didInitialFocusRef` pero refrescaba en **todos** los siguientes → entrar a "Registrar medición" y volver (sin guardar) disparaba un refetch de mediciones + evolución + perfil clínico + lecturas. Lo reportó el usuario. | Se replicó el patrón `wellness-sync` en `src/store/health-sync.ts` (`bumpHealthData()`/`getHealthDataVersion()`). El dashboard guarda `lastSyncedVersionRef = useRef(getHealthDataVersion())` y en focus solo llama `refreshHealthData()` si la versión cambió. Las **3 mutaciones** del módulo bumpean tras un guardado exitoso (antes de `router.back()`): `measurements.tsx` (post medición), `clinical-reading-new.tsx` (post lectura), `clinical.tsx` (update perfil). Así volver de cualquier vista de solo lectura (medidas, evolución, historial, detalle, Bienestar) NO refetchea, y registrar/editar SÍ. Al sumar nuevas mutaciones de Salud, llamar `bumpHealthData()`. Mismo criterio que #153: refrescar en focus SOLO ante mutaciones reales. |

| Crear rutina tenía el diseño viejo (`slate` + `DarkSheetLayout`) y un guardado roto | "Crear rutina" (`CreateRoutineView`) seguía en `slate` con pills de día + `ExerciseFormCard` (TextInputs), mientras "Editar rutina" (`RoutineEditMode`) ya tenía el diseño nuevo `zinc` (DaySlot swipe + `EditExerciseCard` + wheel picker). Peor: su bottom bar estaba en `{false && ...}` y delegaba el guardado al `saveRoutineRef`/`isFormValidRef` del `routine-detail-context`, que **lee el FAB de `MyTabBar`** — pero `app/(tabs)/_layout.tsx` usa `Tabs` nativo, así que **`MyTabBar` no se monta** y crear rutina **no tenía botón de guardar funcional** (mismo patrón que la lección #62 del FAB huérfano). | Las dos pantallas eran ~90% lógica idéntica. Se unificó (DRY, mismo criterio que `FilterPill`): hook `useRoutineEditor` (estado + CRUD de ejercicios por id + `buildPayload`), helpers puros `utils/routine-editor.utils.ts` (`DAYS_MAP`/`calcDayApproxTime`/`routineToDraftDays`/`buildRoutinePayload`) y componente presentacional compartido `RoutineEditorView` (la UI `zinc` nueva). `CreateRoutineView` y `RoutineEditMode` quedaron como **wrappers finos**: solo inyectan init + acción de guardado + animación. El botón de guardar flotante propio de `RoutineEditorView` **arregla el guardado roto** sin depender del FAB. Se conservaron las features exclusivas de crear (zoom desde la card, borrador `useRoutineDraft`, copiar día→día, diálogo "Guardar y activar"). `saveRoutineRef`/`isFormValidRef` del contexto quedan como deuda (sacar la plomería del FAB es un refactor transversal aparte). |
| `DarkSheetLayout` huérfano tras migrar crear a `zinc` | Al reescribir `CreateRoutineView` con `RoutineEditorView`, dejó de importar `DarkSheetLayout` (scaffold `slate` legacy que el proyecto venía migrando a `zinc`). `grep` confirmó que era su **último consumidor** en código (solo quedaban su definición + menciones en docs). | Se borró `src/components/common/DarkSheetLayout.tsx` (mismo criterio "borrar huérfanos tras rediseño" que `RoutineFilters`/`ModuleCard`/`TimerCard`). La mención en `component-library.md` (línea de `ExerciseDetailView`: "ya **no** usa `DarkSheetLayout`") se dejó: sigue siendo cierta. `tsc` 0 errores tras borrar. |

## Offline Fitness/Nutrición

| Error / decisión | Qué pasó | Corrección aplicada |
|---|---|---|
| `expo install` puede timeoutear dejando cambios parciales | `npx expo install expo-sqlite @react-native-community/netinfo` superó el timeout, pero igual modificó `package.json`, `package-lock.json`, `app.json` y dejó paquetes en `node_modules`. Reintentar a ciegas podía duplicar diagnóstico o pisar el estado real. | Después de un timeout de instalación, verificar SIEMPRE `package.json`, lockfile, plugin de `app.json` y existencia en `node_modules` antes de decidir si reintentar. El timeout no implica rollback. |
| AsyncStorage legacy: quitar el import no prueba que se quitó el cache | Al migrar `@user_routine` a SQLite, quedó un `AsyncStorage.setItem('@user_routine')` escondido en `handleActivateRoutine`; recién apareció cuando `tsc --noEmit` marcó el import faltante. | Tras reemplazar una key legacy, hacer `grep` por la key (`@user_routine`, `@nutrition_routine`, `@active_modules`) además de compilar. La migración no está terminada hasta que no quedan escrituras viejas y el logout las borra. |
| Build backend bloqueado por API en ejecución | `dotnet build Backend.slnx` falló copiando `API.exe`/`API.dll` porque había un proceso API vivo bloqueando `bin/Debug`. Eso no era error de compilación del nuevo controller. | Para validar sin matar el proceso del usuario, compilar a un output alternativo (`dotnet build API/API.csproj -o C:\tmp\wellium-api-build`). Si ese build pasa, registrar que el fallo original era lock de archivo, no código. |
| Bundle nutricional offline: no hacer N requests si el backend puede dar snapshot | La descarga offline del plan necesita rutina + detalle/macros/receta de todas las comidas. Hacer un `GET` por comida funciona como fallback, pero degrada peor y falla más fácil a mitad de descarga. | El frontend debe intentar primero `GET /api/offline/nutrition/active-routine` como bundle all-or-nothing y usar el fan-out de `getRoutineMealDetail` solo como fallback cuando no haya bundle activo. |
| PowerShell `-replace`: cuidado con `\`r\`n` literal en C# | Al insertar un `DbSet` con `-replace` y comillas simples, el archivo quedó con los caracteres literales `` `r`n `` dentro de `ApplicationDbContext.cs`, rompiendo el build con `Unexpected character '\`'`. | Para editar C# con PowerShell, usar string con backticks reales en comillas dobles, `Set-Content` por bloque completo, o `apply_patch` cuando el path sea writable. Compilar inmediatamente para detectar este tipo de corrupción textual. |
| Migraciones EF PostgreSQL: no usar tipos de SQL Server | La configuración de `OfflineOperationReceipt.ResultJson` usó `HasColumnType("nvarchar(max)")`; el build pasaba, pero la migración Npgsql quedó con un tipo inválido para PostgreSQL. | En este backend usar tipos PostgreSQL/provider-safe: para JSON/string largo, `HasColumnType("text")` o dejar que Npgsql infiera. Revisar el `.cs` de migración, no solo el build. |

## PWA Fase 4 (shell: manifest + iconos + service worker)

| Error / decisión | Qué pasó | Corrección aplicada |
|---|---|---|
| El plan asumía que `app.json → web.*` genera `manifest.json` con Metro | El plan original (§3) decía "completar `app.json → web` (name, themeColor, ...) **o** `app/+html.tsx`", dando a entender que ambas vías eran equivalentes. Eso es cierto solo con el bundler **Webpack** legacy de Expo; este proyecto usa **Metro** (`expo start --web`), que **no** lee esos campos para emitir un manifest. Tocar `app.json` habría sido config muerta silenciosa (mismo patrón que el `tokenCache` inline de la lección #112: "verificar el consumo real, no lo que el doc/plan asume"). | El manifest se escribió a mano en `public/manifest.json` (`name/short_name/start_url/display/theme_color/background_color/icons`) y `app.json` **no se tocó**. |
| `+html.tsx` parecía web-only "por convención", pero se verificó en el código fuente | La regla de oro del plan dice que los archivos `.web.ts` alcanzan para garantizar "riesgo nativo cero", pero `+html.tsx` no sigue ese patrón de extensión — hacía falta confirmar que expo-router realmente lo excluye del árbol de rutas (y no solo que "nadie lo importa"). | Se verificó en `node_modules/expo-router/build/getRoutesCore.js:96`: `ignoreList = [/^\.\/\+(html|native-intent)\.[tj]sx?$/]` — expo-router **nunca** agrega `+html.tsx` al árbol de rutas (ni web ni nativo); es exclusivo del export estático. Mismo criterio que la lección #112: verificar el mecanismo real en `node_modules`, no asumir por el nombre del archivo. |
| Registro del SW no puede ejecutar código a nivel de render en `+html.tsx` | La lección #127 (Fase 2: `Appearance.setColorScheme` tumbaba toda la app en SSR) ya advertía que `web.output: "static"` pre-renderiza `+html.tsx` en Node. Poner `if ('serviceWorker' in navigator) navigator.serviceWorker.register(...)` como JS normal del componente habría lanzado en el pre-render (sin `navigator` en Node) y roto el export completo. | El registro va como **string** dentro de un `<script dangerouslySetInnerHTML>` — se emite al HTML tal cual, nunca se evalúa durante el pre-render SSR. Se condicionó además a `process.env.NODE_ENV === 'production'` (evaluado en build-time por Metro) para no registrar un SW mientras se corre `expo start --web` en desarrollo (evitaría servir bundles cacheados viejos durante el hot-reload). |
| `scripts/*.js` no tiene entorno Node en el ESLint flat config del proyecto | `eslint-config-expo/flat` reconoce `__dirname`/`process` como globals en archivos de config detectados por nombre (`metro.config.js`, `babel.config.js`, `scripts/reset-project.js` — este último probablemente por no usar `__dirname`), pero un script nuevo (`scripts/generate-pwa-icons.js`) con `__dirname` disparó `no-undef`. | En vez de tocar el `eslint.config.js` compartido para un script one-off, se usó `process.cwd()` en lugar de `path.resolve(__dirname, '..')` — el script ya está documentado para correr desde la raíz del proyecto (`node ./scripts/generate-pwa-icons.js`), así que `cwd()` es equivalente y no requiere cambiar config global por un solo archivo. |
| Generar el ícono maskable sin instalar `sharp` | La Fase 4 necesitaba un ícono maskable (contenido al 80%, centrado, sobre fondo sólido) sin agregar dependencias nuevas. `@expo/image-utils` ya está instalado (dependencia transitiva de `@expo/cli`) pero sin `sharp` en el proyecto, cae automáticamente al path **Jimp puro** (sin binario nativo, corre en el sandbox Windows sin fricción). | Se usó `generateImageAsync` (resize a 80% del lienzo) + `generateImageBackgroundAsync` (fondo `#09090b`) + `compositeImagesAsync` (centrado con offset) — las tres funciones públicas de `@expo/image-utils`, todas con fallback Jimp automático interno. Cero dependencias nuevas instaladas. |

## Regla de oro

NO codifiques por imagen ni por intuición. Primero verificá contrato, DTO real, estado actual del repo y dependencias. Después implementá. Si el backend devuelve algo útil parcialmente, la UI debe degradar con criterio, no esconder datos.

## Regla para controles interactivos anidados

Un control que necesita arrastrar, scrollear o seleccionar NO debe quedar dentro de un `Pressable` activo de la misma zona táctil. Usá este patrón:

1. Estado cerrado: `Pressable` chico que abre el control.
2. Estado abierto: contenedor pasivo (`View`) con el control interactivo adentro.
3. Si el control vive dentro de otro `ScrollView`, habilitar `nestedScrollEnabled` cuando aplique.

Esto evita pelearte con el responder system de React Native. La UI puede verse bien y aun así estar mal compuesta: si el gesto no llega al componente correcto, NO funciona.

## Hidratación (detalle `/health/hydration`)

| Error / decisión | Qué pasó | Corrección aplicada |
|---|---|---|
| Maqueta de Hidratación en azul vs. paleta real | Igual que Sueño/Salud, la maqueta venía 100% en azul (gota, "Nuevo registro →", "1500 ml", bebida resaltada, chevrons). `colors.md` → Salud = `rose-400` (dark). | Todo el azul → `rose-400`. La pantalla espeja `SleepView` (mismo header, `WellnessGroupCard` + `WellnessLogRow` para el historial). No copiar el azul literal: regla de siempre. |
| Reloj de la 3.ª línea del historial sin soporte en el átomo | La maqueta dibuja la hora de cada registro con un ícono de reloj (`🕐 15:00`) en una 3.ª línea. Pero `WellnessLogRow.note` es un `Text` plano (zinc-500) sin ícono, y extender el átomo por una sola pantalla sería sobre-ingeniería (mismo criterio que reusar átomos). | La hora se muestra en `note` = `formatCapturedTime(capturedAt)` (texto plano, sin ícono de reloj). Se priorizó reusar el átomo tal cual (como ya hizo Sueño con su nota) por sobre pintar el ícono decorativo. Si en otra feature el reloj se vuelve necesario, recién ahí se extiende `WellnessLogRow`. |
| Resumen "Hoy" sin endpoint de stats | La card "Hoy" muestra el total de ml del día, pero el contrato no tiene endpoint de agregación (la API devuelve datos brutos; la agregación es frontend). | `useHydrationHistory` calcula `todayMl` sumando los `amountMl` de los logs **ya cargados** cuyo `date === getTodayDateKey()`. La página 1 trae los más recientes (descendente), así que los registros de hoy caen ahí. Mismo criterio que `useWellnessDashboard` (filtra la página 1 por la fecha de hoy). No se inventó un endpoint ni se pidió otra página solo para el total. |
| Registro y detalle de Hidratación como placeholder (fase actual) | El pedido fue implementar **solo** lo de la imagen (la lista) y dejar el resto como placeholder. El form "Nuevo registro" y el detalle por log son fase futura. | `HydrationView` recibe `onRegister?`/`onSelectLog?` opcionales (espejo de `SleepView`): sin ellos la card de registro queda `opacity-60` deshabilitada y las filas no navegan (`showChevron={false}`). La capa de datos (`postHydrationLog`, `submit` del hook) ya existe para cablear el form cuando llegue. Mismo patrón de degradación honesta que Sueño antes de tener `sleep-new`. |

## Ánimo (detalle `/health/mood`)

| Error / decisión | Qué pasó | Corrección aplicada |
|---|---|---|
| Ánimo: espejar el patrón completo de Sueño, no construir de cero | La vista de la maqueta (header + "Registrar ánimo" + "Último registro" con escala + "Historial") + su flujo (form de registro + detalle por log) es estructuralmente **idéntica** a Sueño (`SleepView`/`SleepFormView`/`SleepDetailView` + rutas `sleep`/`sleep-new`/`sleep-detail` + `useSleepHistory`). | Se espejó TODO: `MoodView`/`MoodFormView`/`MoodDetailView` + `MoodLastLogCard`/`MoodRegisterCard`, rutas `mood`/`mood-new`/`mood-detail` y `useMoodHistory` (clon de `useSleepHistory` con `getMoodLogs`/`postMoodLog`). El historial reusa `WellnessGroupCard` + `WellnessLogRow`; las mutaciones llaman `bumpWellnessData()` y las pantallas refrescan en focus solo si la versión cambió (mismo `wellness-sync`). El azul de la maqueta → `rose-400` (regla de siempre). |
| Ánimo: escala de "Último registro"/detalle reusa `SegmentedControl` en solo-lectura | La maqueta dibuja en "Último registro" la escala completa (Muy mal…Muy bien) con el nivel grabado resaltado en azul sólido — visualmente **es** un `SegmentedControl solid`. Tentación de crear un `MoodScale` paralelo (error tipo `FilterPill`). | Se **reusó `SegmentedControl`** (`accent="rose" variant="solid"`, opciones de `MOOD_LEVELS_ORDERED`) con `value={log.mood}` y **`onChange` no-op** = solo-lectura: misma forma visual de la maqueta sin duplicar el control. El precedente de 5 segmentos con labels largos ya estaba en producción (`SleepFormView` calidad), así que no aplica el "Cuándo NO usar >4 opciones". `MOOD_LEVELS_ORDERED` se subió a `wellness.utils` para compartir el orden entre form, último-registro y detalle. |
| Ánimo: form sin validación numérica (no hay duración) | `SleepFormView` valida la duración (1–720) antes de enviar para evitar el 400. El ánimo no tiene ningún campo numérico: `mood` siempre es un enum válido y `date`/`note` no necesitan guard de rango. | `MoodFormView` es el molde de `SleepFormView` **sin** el bloque de Duración ni el estado `validationError`/`MAX_DURATION_MINUTES`: solo Fecha + Estado de ánimo (`SegmentedControl`, default "Bien" como la maqueta) + Nota (máx 120, consistente con Sueño) + CTA. No se agregó validación inventada "para completar el molde". |

## PWA Fase 6 (verificación web: bugs runtime que el análisis estático no ve)

| Error / decisión | Qué pasó | Corrección aplicada |
|---|---|---|
| `@react-native-community/datetimepicker` no tiene soporte web → el picker de fecha no se monta en web | El campo "Fecha de nacimiento" (`BasicInfoStep1.tsx`) no aparecía en web. Doble causa: (1) `@react-native-community/datetimepicker@8.4.4` **no trae build web** (verificado: cero archivos `.web.js`, `index.js` importa el módulo nativo inexistente en web); (2) el render branch era `{Platform.OS === 'android' && !showDatePicker && (...)}` + `{(showDatePicker || Platform.OS === 'ios') && (<DateTimePicker/>)}`, y `showDatePicker` arranca en `Platform.OS === 'ios'` (false en web) → **en web ninguna de las dos ramas se monta**. Lo reportó el usuario en el smoke test de Fase 6. | Fix aditivo (bundle nativo intacto): se agregó una rama `{Platform.OS === 'web' && (<input type="date" .../>)}` **antes** de las dos existentes (que ya excluían web por sus condiciones). react-native-web pinta el `<input>` como DOM real vía react-dom; el `tsconfig` de Expo incluye la lib `DOM` así que el intrínseco `<input>` tipa sin fricción. Helpers puros `toDateInputValue`/`parseDateInputValue` (formato `YYYY-MM-DD` local, construyendo la Date por componentes para evitar el shift UTC de `new Date('YYYY-MM-DD')`). `colorScheme:'dark'` en el style para que el calendario nativo del browser salga oscuro. **Deuda (RESUELTA):** el MISMO patrón roto vivía en los 4 forms de Salud (`SleepFormView`/`MoodFormView`/`HydrationFormView`/`ClinicalReadingFormView`) — en web caían a la rama android del `DateTimePicker`. Se atomizó `common/DatePickerField` (`.tsx` nativo con el DateTimePicker ios/android + `.web.tsx` con `<input type="date">`), Metro resuelve por extensión y el bundle web ni importa el paquete nativo. Los 4 forms perdieron su estado `showPicker` y el import de `DateTimePicker`; solo pasan `value`/`onChange`/`formatValue`/`accentColor` (el único punto que divergía era el formatter: `formatFullDate(toDateKey(d))` en Sueño/Ánimo/Hidratación vs `formatDate` en Clínico). El onboarding (`BasicInfoStep1`) **NO** se migró: su layout de fecha diverge (picker inline siempre visible, sin card+toggle) → forzarlo al átomo habría sido meter a la fuerza un caso distinto. `tsc`/ESLint/`expo export web` verdes. Regla: al degradar una API nativa en web, buscar TODOS los consumidores del mismo componente; si son 3+, atomizar con `.web.tsx` en vez de repetir el fix inline. |
| `@react-native-picker/picker` (WheelPicker) se pinta como `<select>` blanco ilegible en web | En "Datos físicos" (`BasicInfoStep2`) los selectores de Peso y Altura salían como **cajas blancas rotas** (y al abrir, una lista blanca gigante). Causa: `WheelPicker` (`common/WheelPicker.tsx`) usa `@react-native-picker/picker`, que en web se renderiza como `<select>` HTML con el fondo claro del navegador; el `style={{ color: '#ffffff' }}` deja **texto blanco sobre fondo blanco** = invisible. Lo reportó el usuario con captura en el smoke test de Fase 6. | Mismo patrón que `DatePickerField`: se creó `common/WheelPicker.web.tsx` que reemplaza el wheel por un **slider nativo** (`<input type="range">`, la forma web natural de elegir un número en un rango, arrastrable en desktop). El valor grande y la unidad se muestran igual que en nativo y se actualizan al arrastrar; `accentColor` = color del acento (mono/lime/amber). Se **exportó** `WheelPickerProps` en el `.tsx` nativo (antes era `interface` sin export) para type-only import desde el web. Bonus: el mismo átomo arregla el `WheelPicker` de la duración en el onboarding de Fitness (`FitnessConfigStep`, "Elegir tiempo"), que tenía el mismo bug en web. `tsc`/ESLint/`expo export web` verdes. Regla (van 3 casos): las **APIs de UI nativas de Expo/RN sin build web decente** (`DateTimePicker`, `@react-native-picker/picker`, `expo-camera` barcode, `Haptics`) se resuelven con split `.web.tsx` usando el equivalente DOM nativo (`<input type="date">`, `<input type="range">`), NO tratando de estilar el componente nativo para que "se vea bien" en web. |
| El plan §2.2 asumió que las migraciones corrían igual en web dentro de `withExclusiveTransactionAsync` | El plan decía "`ALTER TABLE ADD COLUMN` (v2) sí corre igual en ambas plataformas dentro de `withExclusiveTransactionAsync`". **Falso, verificado en `node_modules/expo-sqlite/src/SQLiteDatabase.ts:170-172`:** esa API hace `if (Platform.OS === 'web') throw new Error('withExclusiveTransactionAsync is not supported on web')`. Usa una **conexión separada** (`Transaction.createAsync`) para dar aislamiento exclusivo, algo que el backend web (`AccessHandlePoolVFS`) no soporta. Las 2 migraciones de `src/offline/db.ts` la usaban → la capa offline en web **reventaba en la primera migración** (v0→v1), no en la v2. El error saltó recién en runtime (al abrir la DB en el logout); `tsc`/ESLint/`expo export` pasaban todos en verde porque es un throw de runtime, no un error de tipos. Lo reportó el usuario ("withExclusiveTransactionAsync is not supported on web" al ir a cerrar sesión). | Se agregó un helper `runMigrationTransaction(db, task)` en `db.ts`: **nativo queda IDÉNTICO** (`withExclusiveTransactionAsync`, con `tx`), y **web** cae a `withTransactionAsync` (BEGIN/COMMIT/ROLLBACK sobre la misma conexión, atómico, sí soportado en web). Es seguro porque las migraciones corren **al abrir la DB, antes de que `dbPromise` resuelva** → no hay concurrencia, así que el aislamiento por conexión separada que da la variante exclusiva no aporta nada en ese momento. El `task` recibe un `Pick<SQLiteDatabase, 'execAsync'>` (tanto `Transaction` nativo como `db` web lo satisfacen). `tsc`/ESLint verdes; el fix real se valida en el smoke test web (migraciones v1/v2 sin throw). **Lección madre:** un throw `is not supported on web` NO lo detecta ningún check estático — solo aparece corriendo la app en el navegador. El Carril A (typecheck/lint/export) valida bundling y estructura, NO comportamiento runtime; por eso el Carril B (smoke test en browser) es obligatorio, no opcional. |
